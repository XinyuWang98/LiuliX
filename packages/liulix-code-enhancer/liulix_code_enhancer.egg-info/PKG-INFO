Metadata-Version: 2.1
Name: liulix-code-enhancer
Version: 1.0.0
Summary: AST-based Python code enhancer for data analysis
Home-page: https://github.com/liulix/code-enhancer
Author: LiuliX Team
Author-email: team@liulix.io
License: UNKNOWN
Platform: UNKNOWN
Classifier: Programming Language :: Python :: 3
Classifier: Programming Language :: Python :: 3.8
Classifier: Programming Language :: Python :: 3.9
Classifier: Programming Language :: Python :: 3.10
Classifier: Programming Language :: Python :: 3.11
Classifier: License :: OSI Approved :: MIT License
Classifier: Operating System :: OS Independent
Classifier: Topic :: Software Development :: Code Generators
Classifier: Topic :: Software Development :: Quality Assurance
Requires-Python: >=3.8
Description-Content-Type: text/markdown
Provides-Extra: py38
Provides-Extra: dev
License-File: LICENSE

# LiuliX Code Enhancer

[![Python Version](https://img.shields.io/badge/python-3.8+-blue.svg)](https://www.python.org/downloads/)
[![License](https://img.shields.io/badge/license-MIT-green.svg)](LICENSE)

**AST级别的Python代码安全增强器**，专为数据分析场景设计。

## 特性

- ✅ **零依赖**：仅使用Python标准库（Python 3.9+）
- ✅ **跨平台**：支持Web(Pyodide)、PC(CPython)、CLI、服务端
- ✅ **精确增强**：基于AST语法树，零误伤
- ✅ **自动防护**：自动注入空数据检查、索引保护、异常处理

## 快速开始

### 安装

```bash
pip install liulix-code-enhancer
```

Python 3.8用户需要额外安装astor：
```bash
pip install liulix-code-enhancer[py38]
```

### 使用示例

```python
from liulix_enhancer import CodeEnhancer

# 原始AI生成的代码
raw_code = """
result = df['income'].values[0]
summary = f"Top category: {df['category'].value_counts().index[0]}"
"""

# 创建增强器
enhancer = CodeEnhancer(
    columns=['income', 'category'],
    df_name='df'
)

# 增强代码
result = enhancer.enhance(raw_code)

if result['success']:
    print("增强后的代码:")
    print(result['code'])
    print(f"\n应用的规则: {result['stats']}")
else:
    print(f"增强失败: {result['error']}")
```

### 命令行使用

```bash
# 从文件增强
liulix-enhance script.py --columns '["col1", "col2"]'

# 标准输入
echo "x = arr[0]" | liulix-enhance --columns '[]'
```

## 增强规则

### 1. 空数据检查
```python
# 自动注入
if len(df) == 0:
    raise ValueError("输入数据为空，无法进行分析")
```

### 2. 列存在性验证
```python
# 自动检查
required_cols = ['income', 'category']
missing = [c for c in required_cols if c not in df.columns]
if missing:
    raise ValueError(f"缺少必需列: {missing}")
```

### 3. 数组索引保护（精确）
```python
# 仅保护简单变量
arr[0]  →  (arr[0] if len(arr) > 0 else None)

# 不保护属性访问
df.index[0]  →  df.index[0]  # 保持不变
```

### 4. 全局异常捕获
自动包裹try-catch，将IndexError、KeyError转换为友好错误提示。

### 5. GroupBy增强
检测groupby操作，自动验证分组列唯一值数量。

## 技术细节

### 架构
- **Layer 1**: 核心Python包（本包）
- **Layer 2**: 平台适配层（Pyodide/Native Python）
- **Layer 3**: 业务接口（TypeScript/Rust）

### 为什么选择AST而非正则？

| 方案     | 准确率 | 误伤率 | 可扩展性 |
| -------- | ------ | ------ | -------- |
| 正则匹配 | 60%    | 40%    | 低       |
| AST重写  | 95%+   | <5%    | 高       |

**正则方案的问题**：
```python
# 错误示例：正则无法区分上下文
value_counts.index[0]
# 错误替换为 → value_counts.((index[0]...))  SyntaxError!
```

**AST方案的优势**：
- 精确识别语法结构
- 可区分Name[int] vs Attribute[int]
- 易于扩展新规则

## 开发

### 本地安装
```bash
git clone https://github.com/liulix/code-enhancer.git
cd code-enhancer
pip install -e .[dev]
```

### 运行测试
```bash
pytest tests/ -v --cov=liulix_enhancer
```

### 构建发布
```bash
python -m build
twine upload dist/*
```

## 兼容性

- Python 3.8+
- Pyodide (Web)
- PyO3 (Rust FFI)

## License

MIT License - 详见 [LICENSE](LICENSE)

## 致谢

本项目由 [LiuliX](https://liulix.io) 团队开发，旨在提高AI生成代码的健壮性。


