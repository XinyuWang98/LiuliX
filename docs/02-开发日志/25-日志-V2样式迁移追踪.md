# V2 样式迁移追踪清单

**创建日期**: 2025-12-31  
**目的**: 系统性追踪 V2 页面各模块向 LiuliX "Ascension" 视觉规范的迁移进度

---

## 1. 迁移状态总览

| 模块 | 状态 | 最后更新 | 备注 |
|:---|:---:|:---:|:---|
| **VirtualDataGridV2** | ✅ 完成 | 2025-12-31 | 高亮色+字体+微型图+列对齐 |
| **DataCleaner** | ✅ 完成 | 2025-12-31 | 组件拆分+玻璃态 |
| **BottomPanel** | ✅ 完成 | 2025-12-31 | 底部面板玻璃态 |
| **InsightChainFlow** | ⏳ 待做 | - | 洞察分析模块 |
| **ForestExplorer** | ⏳ 待做 | - | 下钻森林视图 |
| **ReportGenerator** | ⏳ 待做 | - | 报告生成模块 |
| **ContentPanel** | ✅ 完成 | 2025-12-31 | 布局修复 |
| **ProjectCardGrid** | ⏳ 待检查 | - | 项目卡片网格 |

---

## 2. 样式检查清单

每个模块迁移时，必须逐项核对：

### 2.1 玻璃态容器
- [ ] 背景色使用 `var(--glass-surface)` 而非硬编码
- [ ] 模糊度使用 `var(--blur-medium)` (16px) 或 `var(--blur-heavy)` (30px)
- [ ] 边框使用 `var(--glass-border)` 而非 `var(--border)`
- [ ] 圆角使用 `var(--radius-l)` 或 `var(--radius-xl)`

### 2.2 高亮与交互
- [ ] 高亮色使用蓝色 `rgba(10, 132, 255, 0.08/0.15)` 而非黄色
- [ ] hover 效果使用 `var(--bg-hover)` 或 `rgba(255, 255, 255, 0.03)`
- [ ] 选中态使用 `var(--primary)` 相关变量

### 2.3 字体与文本
- [ ] 字体大小使用 `var(--fs-sm)` / `var(--fs-md)` / `var(--fs-lg)`
- [ ] 文本颜色使用 `var(--text-primary)` / `var(--text-secondary)` / `var(--text-tertiary)`
- [ ] 等宽字体使用 `var(--font-mono)` 而非硬编码

### 2.4 微型图与图表
- [ ] 背景色使用 `var(--primary)` (蓝色)
- [ ] 柱状图透明度约 80%
- [ ] hover 时 opacity 提升至 100%

### 2.5 代码合规
- [ ] 无硬编码颜色值
- [ ] 无魔法数字（像素值定义为常量）
- [ ] 中文注释完整
- [ ] i18n 覆盖所有用户文案

---

## 3. CSS 变量速查（核心）

### 3.1 玻璃态变量
| 变量 | 值 | 用途 |
|:---|:---|:---|
| `--glass-surface` | `rgba(255, 255, 255, 0.03)` | 容器背景 |
| `--glass-border` | `rgba(255, 255, 255, 0.15)` | 边框 |
| `--glass-border-highlight` | `rgba(255, 255, 255, 0.4)` | 顶部高光边框 |
| `--blur-medium` | `16px` | 标准模糊 |
| `--blur-heavy` | `30px` | 重模糊（面板） |

### 3.2 颜色变量
| 变量 | 值 | 用途 |
|:---|:---|:---|
| `--primary` | `#0A84FF` | 主色/高亮/微型图 |
| `--text-primary` | `#FFFFFF` | 主文本 |
| `--text-secondary` | `rgb(142, 142, 147)` | 次文本（灰色） |
| `--text-tertiary` | `rgba(180, 200, 255, 0.6)` | 辅助文本 |

### 3.3 字体变量
| 变量 | 值 | 用途 |
|:---|:---|:---|
| `--fs-sm` | `13px` | 小字体（表格单元格） |
| `--fs-md` | `15px` | 中字体 |
| `--fs-lg` | `18px` | 大字体 |
| `--font-mono` | `'SF Mono', Consolas, monospace` | 代码字体 |

---

## 4. 迁移操作指南

### 4.1 新模块迁移步骤

1. **检查现有 liulix 组件**
   ```bash
   # 查看可用组件
   ls src/components/common/liulix/
   ```

2. **优先使用 liulix 类**
   ```tsx
   // ✅ 推荐
   <div className="liuli-glass">...</div>
   
   // ❌ 不推荐
   <div style={{ background: 'rgba(...)' }}>...</div>
   ```

3. **次选使用 CSS 变量**
   ```css
   .my-container {
       background: var(--glass-surface);
       backdrop-filter: blur(var(--blur-medium));
       border: 1px solid var(--glass-border);
   }
   ```

4. **浏览器验证**
   - 对比 http://localhost:5173/#/design 效果
   - 检查玻璃态透明度和模糊
   - 确认颜色无硬编码

---

## 5. 已知问题与待办

| 问题 | 优先级 | 状态 |
|:---|:---:|:---:|
| liulix 缺少 LiuliTable 组件 | P3 | 待规划 |
| liulix 缺少 LiuliPanel 组件 | P3 | 待规划 |
| InsightChainFlow 未迁移 | P2 | 待做 |
| ReportGenerator 未迁移 | P2 | 待做 |

---

**维护说明**: 每次完成模块迁移后，更新本文档的"迁移状态总览"表格。
