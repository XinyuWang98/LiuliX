# 文件拆分日志

## 1. 拆分记录 (2025-12-21)

### 1.1 `src/components/layout/LeftSidebar.tsx`

**【文件分析卡片】**
- **文件路径**：`src/components/layout/LeftSidebar.tsx`
- **当前行数**：777行
- **主要逻辑**：负责侧边栏的项目管理、文件树渲染、文件上传、右键菜单、AI状态展示、重命名/删除交互。
- **行数超标原因**：
  1. **UI组件耦合**：文件树渲染 (`renderTree`)、项目列表渲染 (`projects.map`)、右键菜单 (`ContextMenu`) 全部写在同一个文件中。
  2. **状态逻辑复杂**：包含大量 `useState` (expanded, renaming, contextMenu, upload...) 和 `useEffect` (AI status check)。
  3. **内联样式混杂**：虽然提取了部分 CSS，但仍有大量内联样式逻辑。

**【拆分方案建议】**

**方案 A：功能组件拆分 (推荐)**
将大组件拆分为独立的子组件，保持 `LeftSidebar` 作为容器和状态管理者。
- `src/components/layout/sidebar/SidebarHeader.tsx`: 顶部标题、搜索框、添加按钮。
- `src/components/layout/sidebar/ProjectList.tsx`: 项目列表渲染逻辑。
- `src/components/layout/sidebar/FileTreeItem.tsx`: 单个文件/文件夹节点的渲染 (递归或列表项)。
- `src/components/layout/sidebar/SidebarContextMenu.tsx`: 右键菜单组件。
- `src/components/layout/sidebar/EmptyState.tsx`: 无项目时的空状态。

**方案 B：逻辑 Hook 提取**
仅提取逻辑，保留 JSX。
- `useSidebarLogic.ts`: 包含所有 `useState` 和事件处理函数。

**【预估收益】**
- 主文件行数降至 < 200 行。
- `FileTreeItem` 可复用，性能优化更方便 (React.memo)。
- 维护性大幅提升。

**【执行结果】**
- ✅ 已执行方案 A (组件拆分)
- **拆分前**: 780行 (UI耦合严重)
- **拆分后**: ~480行 (合规)
- **产物**:
  - `src/components/layout/LeftSidebar.tsx` (核心状态容器)
  - `src/components/layout/sidebar/SidebarHeader.tsx`
  - `src/components/layout/sidebar/ProjectItem.tsx`
  - `src/components/layout/sidebar/FileTreeItem.tsx`
- **验证**: `npm run type-check` 通过。

---

### 1.2 `src/components/insights/InsightChainFlow.tsx`

**【文件分析卡片】**
- **文件路径**：`src/components/insights/InsightChainFlow.tsx`
- **当前行数**：470行
- **主要逻辑**：负责洞察生成的核心流程，包括DuckDB查询、数据采样、AI调用（DeepSeek/Local）、Pyodide执行、结果渲染。
- **行数超标原因**：
  1. **逻辑高内聚**：集成了数据加载、AI交互、Python执行、结果解析、错误处理等所有逻辑。
  2. **UI渲染混杂**：包含加载状态、错误提示、卡片网格、Mock数据渲染等大量JSX。
  3. **状态管理复杂**：Strict Mode防重、AbortController管理、智能刷新依赖判断。

**【拆分方案建议】**

**方案 A：按职责拆分 (推荐)**
1. **`useInsightLoader.ts` Hook**：提取洞察加载核心逻辑（DuckDB/AI/Pyodide）。
2. **`useInsightRefresh.ts` Hook**：提取智能刷新和防重逻辑。
3. **`InsightCardGrid.tsx` 组件**：提取假设卡片列表渲染。
4. **`InsightChainFlow.tsx` 主组件**：保留顶层布局和Mock节点逻辑。

**【执行结果】**
- ✅ 已执行方案 A
- **拆分前**：470行 (逼近红线，且逻辑混杂)
- **拆分后**：195行 (主组件大幅瘦身)
- **产物**：
  - `src/hooks/useInsightLoader.ts` (230行)
  - `src/hooks/useInsightRefresh.ts` (91行)
  - `src/components/insights/InsightCardGrid.tsx` (128行)
  - `src/components/insights/InsightChainFlow.tsx` (195行)
- **验证**：WebLLM 本地模型集成无缝接入，逻辑清晰。

---

### 1.3 `src/components/AIConfigModal.tsx`

**【文件分析卡片】**
- **文件路径**：`src/components/AIConfigModal.tsx`
- **当前行数**：625行 (拆分前)
- **主要逻辑**：AI配置模态框，包含API优先级拖拽、本地模型开关、Skills架构配置、性能配置等5大功能模块
- **行数超标原因**：功能集中在单一组件，缺少模块拆分

**【拆分方案建议】**

**方案 A：提取Skills配置 + 性能配置 (已采用)**
1. **`SkillsConfigSection.tsx`**：提取Skills架构全局/模块/高级开关UI (~150行)
2. **`PerformanceConfigSection.tsx`**：提取性能配置滑块UI (~80行)
3. **`AIConfigModal.tsx` 主组件**：保留模态框框架、API优先级、本地模型开关 (~395行)

**【执行结果】**
- ✅ 已执行方案 A
- **拆分前**：625行 (超限+125行)
- **拆分后**：
  - `src/components/AIConfigModal.tsx` → 493行 ✅
  - `src/components/settings/SkillsConfigSection.tsx` → 145行 ✅  
  - `src/components/settings/PerformanceConfigSection.tsx` → 107行 ✅
- **验证**：`npm run type-check` 通过，所有文件符合500行限制
- **日期**：2025-12-23

---

### 1.4 `src/services/aiService.ts` (预警分析)

**【文件分析卡片】**
- **文件路径**：`src/services/aiService.ts`
- **当前行数**：425行 (临近警戒线)
- **主要逻辑**：负责AI服务多通道调用（Gemini/Claude/DeepSeek/Grok）、Prompt构造、数据清洗规则混合实现、API请求代理封装。
- **行数超标原因**：
  1. **职责过载 (High Cohesion)**：集成了通用传输层 (`askAI`)、专用通道方法 (`askAICleaning`)、以及具体的业务规则逻辑 (`generateCleaningSuggestions` 包含大量硬编码规则)。
  2. **配置耦合**：包含了多模型配置对象 `CONFIG` 和优先级逻辑。

**【拆分方案建议】**

**方案 A：业务逻辑剥离 (推荐)**
1. **`src/services/logic/cleaningRules.ts`**: 将 `generateCleaningSuggestions` (约100行) 中的规则和Prompt匹配逻辑剥离。
2. **`src/services/transport/aiTransport.ts`**: 将 `ky` 请求封装和 `CONFIG` 配置剥离。
3. **`aiService.ts`**: 仅保留作为对外统一Facade接口，转发调用。

**【执行状态】**
- **状态**：⚠️ 监控中
- **说明**：当前暂未严格超标 (<500)，但已非常接近。建议在下次修改该文件时执行拆分。

---

### 1.5 `src/utils/localModelQueue.ts` (应用户要求分析)

**【文件分析卡片】**
- **文件路径**：`src/utils/localModelQueue.ts` (曾被规划为 `aiQueue.ts`)
- **当前行数**：112行
- **主要逻辑**：
  - `LocalModelQueue` 类：单例模式，管理本地模型的串行执行。
  - `enqueue` 方法：支持优先级 (`high`/`normal`) 的请求入队。
  - `processNext` 方法：递归调度执行，含 100ms 间隔保护 GPU。
- **行数超标原因**：
  - ✅ **未超标** (112 < 500)。
  - 代码结构清晰，职责单一（仅负责本地模型队列）。

**【拆分/重构建议】**
- **建议**：无需拆分。
- **备注**：用户提及的 `aiQueue.ts` 变更原计划是将此文件泛化。但经排查，API模式无需队列，故此文件应保持专注于 "Local Model Protection"，无需泛化或重命名，避免引入不必要的复杂性。

**【执行状态】**
- **状态**：✅ 已分析 (无需变更)
- **日期**：2025-12-25

---

### 1.6 `src/components/VirtualDataGrid.tsx`

**【文件分析卡片】**
- **文件路径**：`src/components/VirtualDataGrid.tsx`
- **当前行数**：637行 (拆分前)
- **主要逻辑**：虚拟化数据表格，包括表头渲染、数据行虚拟滚动、统计图表微件、类型转换交互。
- **行数超标原因**：
  1. **复杂子组件内聚**：`MiniHistogram`, `MiniBarChart`, `SingleValueIndicator` 等可视化组件直接定义在文件内。
  2. **Portal逻辑**：引入了 Portal 下拉菜单及相关状态管理。
  3. **样式与事件处理**：包含大量内联样式计算和事件响应逻辑。

**【拆分方案建议】**

**方案 A：可视化子组件提取 (已执行)**
1. **`src/components/datagrid/MiniHistogram.tsx`**：提取直方图微件。
2. **`src/components/datagrid/MiniBarChart.tsx`**：提取条形图微件。
3. **`src/components/datagrid/SingleValueIndicator.tsx`**：提取单值指示器。
4. **`src/types/duckdb.ts`**：补充 `ColumnStats` 类型定义以支持跨组件引用。

**【执行结果】**
- ✅ 已执行方案 A
- **拆分前**：637行
- **拆分后**：~380行
- **产物**：
  - `src/components/VirtualDataGrid.tsx`
  - `src/components/datagrid/MiniHistogram.tsx`
  - `src/components/datagrid/SingleValueIndicator.tsx`
- **日期**：2025-12-27

---

## 2. 拆分记录 (2025-12-29)

### 2.1 `src/db/duckdbEngine.ts`

**【文件分析卡片】**
- **文件路径**：`src/db/duckdbEngine.ts`
- **当前行数**：578行 (拆分前)
- **主要逻辑**：DuckDB WASM引擎的单例类，负责数据库初始化、CSV摄入、查询、统计、数据清洗操作。
- **行数超标原因**：
  1. **单一类18个方法**：混合了初始化逻辑、数据摄入、查询、统计、清洗等多种职责。
  2. **`getColumnStats` 方法超过200行**：单个方法过于复杂。
  3. **职责不清**：Singleton模式与业务逻辑耦合。

**【拆分方案】**

**方案：按职责分层拆分 (已执行)**
1. **`duckdbEngine.ts`**：Singleton核心引擎、初始化、连接管理 (~200行)
2. **`duckdbIngestion.ts`**：CSV分析和摄入功能 (~150行)
3. **`duckdbQuery.ts`**：查询、导出、列信息获取 (~80行)
4. **`duckdbStats.ts`**：列统计、直方图、分布数据 (~220行)
5. **`duckdbCleaning.ts`**：清洗SQL、重置表、修改列类型 (~140行)

**【执行结果】**
- ✅ 已完成
- **拆分前**：578行 (单一文件，职责混杂)
- **拆分后**：
  - `src/db/duckdbEngine.ts` → 200行 ✅
  - `src/db/duckdbIngestion.ts` → 150行 ✅
  - `src/db/duckdbQuery.ts` → 80行 ✅
  - `src/db/duckdbStats.ts` → 220行 ✅
  - `src/db/duckdbCleaning.ts` → 140行 ✅
- **验证**：
  - ✅ 编译无错误
  - ✅ DuckDB初始化正常
  - ✅ 数据加载和查询正常
  - ✅ transactions.csv (103,127行) 成功加载并显示
- **日期**：2025-12-29

---

### 2.2 `src/components/cleaning/hooks/useSuggestionGeneration.ts`

**【文件分析卡片】**
- **文件路径**：`src/components/cleaning/hooks/useSuggestionGeneration.ts`
- **当前行数**：518行 (拆分前)
- **主要逻辑**：React Hook，负责数据清洗建议的生成、刷新和管理。
- **行数超标原因**：
  1. **`generateSuggestions` 函数超过360行**：包含数据获取、规则检测、AI调用、缓存管理等复杂逻辑。
  2. **状态管理复杂**：AbortController、缓存机制、防重复生成等多种状态。
  3. **逻辑耦合**：业务逻辑与Hook生命周期混在一起。

**【拆分方案】**

**方案：提取生成逻辑为Utils模块 (已执行)**
1. **`useSuggestionGeneration.ts`**：Hook骨架、状态管理、生命周期 (~260行)
2. **`suggestionGenerators.ts`**：数据获取、规则检测、AI调用逻辑 (~250行)
3. **`suggestionFilters.ts`**：建议合并、排序、去重工具 (~70行)

**【执行结果】**
- ✅ 已完成
- **拆分前**：518行 (逻辑混杂，难以维护)
- **拆分后**：
  - `src/components/cleaning/hooks/useSuggestionGeneration.ts` → 260行 ✅
  - `src/components/cleaning/utils/suggestionGenerators.ts` → 250行 ✅
  - `src/components/cleaning/utils/suggestionFilters.ts` → 70行 ✅
- **验证**：
  - ✅ 编译无错误
  - ✅ 规则建议生成正常
  - ✅ AI建议生成正常（成功生成4条AI建议）
  - ✅ 缓存机制正常
  - ✅ 文件切换时自动重试机制正常
- **日期**：2025-12-29

---

### 2.3 `src/services/aiService.ts` (保留原样)

**【文件分析卡片】**
- **文件路径**：`src/services/aiService.ts`
- **当前行数**：512行 (边界值)
- **主要逻辑**：AI服务核心，负责多模型调用、清洗建议生成、洞察分析。
- **保留原因**：按用户要求保留为Re-export层，确保向后兼容。

**【拆分策略】**
- **状态**：⚠️ 暂不拆分
- **建议**：后续可考虑拆分为 `ai/aiCore.ts`、`ai/aiCleaning.ts`、`ai/aiInsight.ts`，原文件作为兼容层Re-export。
- **日期**：2025-12-29
