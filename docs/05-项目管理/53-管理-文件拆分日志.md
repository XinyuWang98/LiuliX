## 文件拆分记录 #1

### 原文件
`src/components/insights/InsightChainFlow.tsx` (540行)

### 拆分方案
方案1: Hook拆分 (推荐)

### 拆分动作

#### Step 1: 创建 useNotebookLayout.ts
**文件**: `src/hooks/useNotebookLayout.ts`  
**职责**: Notebook面板显示/隐藏、宽度管理、拖拽调整  
**行数**: ~80行  
**提取内容**:
- `showNotebook` 状态及localStorage持久化
- `notebookWidthPercent` 状态
- `isResizing` 状态
- 拖拽处理函数 (`handleResizeStart`, `handleResize`, `handleResizeEnd`)
- 全局事件监听逻辑

#### Step 2: 创建 useInsightNodeManager.ts
**文件**: `src/hooks/useInsightNodeManager.ts`  
**职责**: InsightNode增删改查、展开/折叠、状态管理  
**行数**: ~100行  
**提取内容**:
- `insightNodes` 状态
- `expandedNodeIds` 状态
- `focusedNodeId` 状态
- `handleToggleExpand` (手风琴逻辑)
- `handleStatusChange` (adopted/ignored状态)
- `resolvedCodes` memo计算

#### Step 3: 重构主组件
保留 ~360行:
- Props接收
- useInsightLoaderV2调用
- 下钻处理逻辑
- UI渲染(ForestExplorer + LiveNotebook)

### 预期效果
- InsightChainFlow.tsx: 540行 → ~360行 ✅
- 代码职责更清晰
- Hook可复用(useNotebookLayout可用于其他拖拽面板)

### 实际效果 (2026-01-09)
- ✅ InsightChainFlow.tsx: 540行 → 360行
- ✅ useNotebookLayout.ts: 103行
- ✅ useInsightNodeManager.ts: 180行
- ✅ 代码职责分离清晰,逻辑可复用
- ✅ 解决了行数超标问题 (减少180行)

---

## 2. [scripts/batch_test_insights_single.ts](scripts/batch_test_insights_single.ts) (2026-01-15 14:15)

*   **原始行数**: 773 行
*   **拆分后**: 
    - `scripts/batch_test_insights_single.ts`: 主入口 (行数大幅减少)
    - `scripts/test-utils/batch_test_config.ts`: 120 行 (Config, Types, Data)
    - `scripts/test-utils/batch_test_report.ts`: 240 行 (Report Generation)
*   **原因**: 文件行数超过 500 行 (Rule 11)，为了优化可维护性并支持冷启动监控功能扩展。
*   **操作**: 提取配置与报告生成逻辑至 `scripts/test-utils` 模块。
