
# 2025-12-18 Type Error Log

## 场景
Refactoring DataViewer.tsx to fix race condition using useRef.

## 问题描述
Introduced lint errors by using useRef without importing it, and implicit any types for loop variables.
1. Cannot find name 'useRef'.
2. Parameter 'file' implicitly has an 'any' type.
3. Parameter 'f' implicitly has an 'any' type.

## 原因分析
Hastily applied useRef pattern without checking imports and TypeScript types. project.files map callback needs explicit typing, and findIndex callback needs explicit typing or inference checking.

## 解决方案
1. Import useRef from 'react'.
2. Import ProjectFile from utils/projectUtils.
3. Add : ProjectFile type annotation to file in map.
4. Inference should handle f in findIndex, but if not, explicit type will fix it.

## 策略标签
#类型修正 #依赖缺失

---

# 2025-12-18 质量评分阈值不一致修复

## 场景
质量评分徽章显示逻辑

## 问题描述
SmartFileTabBar 的徽章颜色阈值与 DataQualityService 的评分标准不一致：
- DataQualityService: 85+ 健康(绿), 60-84 警告(红), <60 严重(红)
- SmartFileTabBar (修复前): 80+ 绿, 60-79 红, <60 红
- 实际冲突: 82 分显示绿色徽章，但健康状态实际为警告(红色)

## 原因分析
代码重构时未统一管理阈值常量，两处分别定义了颜色映射规则。

## 解决方案
将 SmartFileTabBar.tsx 的 getScoreColor 函数阈值从 80 修正为 85：
```typescript
if (score >= 85) return 'var(--success)'; // 从 80 改为 85
```

## 策略标签
#逻辑重构

---

# 2025-12-18 质量评分不显示修复

## 场景
质量评分徽章不显示在文件标签页上

## 问题描述
控制台没有 [BatchCheck] 日志，质量评分计算未执行。
根因：batchAssess useEffect 依赖数组 [project?.id, project?.files.length, t] 缺少对 tableName 的监听。
当 DuckDB 重新 ingest 后更新 tableName 时，files.length 没变，所以 batchAssess 不会重新执行。

## 原因分析
执行流程：
1. 页面加载，batchAssess 尝试用旧 tableName (t_1765702707436_working) 查询 -> 表不存在
2. loadData 重新 ingest，创建新表 (t_1766063369421_working)
3. 更新 project state 中的 tableName
4. 但 batchAssess 的依赖 [project?.id, files.length, t] 都没变化 -> 不会重新执行

## 解决方案
添加对所有文件 tableName 的依赖监听：
```typescript
}, [
    project?.id, 
    project?.files.length, 
    project?.files.map(f => f.data.tableName).join(','), // 监听 tableName 变化
    t
]);
```

## 策略标签
#逻辑重构

---

# 2025-12-18 重构导致数据加载失败

## 场景
DataViewer.tsx 重构后，数据表显示元数据而非实际数据

## 问题描述
错误流程：
1. 尝试使用旧 tableName (t_1765702707436_working) -> 表不存在
2. 准备重新 ingest -> 调用 DataLoadingService.loadWithDuckDB
3. **无法获取原始文件对象** -> DuckDB 失败
4. 回退到 Pyodide -> 也失败
5. 最终显示文件元数据

## 原因分析
重构时删除了关键回退逻辑：
- 原代码：检查 ile.data.originalFile -> 检查 ile.data.rawFile -> **从 file.data.data (JSON) 重建 CSV**
- 新代码：只检查 ile.data.rawFile || file.data.file -> 直接抛出错误

旧文件的 rawFile 对象已丢失（浏览器刷新后 File 对象无法持久化），必须从 JSON 数据重建！

## 解决方案
在 dataLoadingService.ts 的 loadWithDuckDB 中添加 CSV 重建逻辑：
```typescript
// 优先使用原始文件
const rawFile = file.data.rawFile || file.data.originalFile || file.data.file;

if (!rawFile) {
    // 回退：从 JSON 数据重建 CSV
    const data = file.data.data;
    if (!data || data.length === 0) {
        throw new Error('无法获取原始文件对象且无数据可重建');
    }
    
    const headers = Object.keys(data[0]);
    let csvContent = headers.join(',') + '\\n';
    data.forEach((row: any) => {
        csvContent += headers.map(h => {
            const val = row[h];
            return val === null || val === undefined ? '' : String(val);
        }).join(',') + '\\n';
    });
    
    const blob = new Blob([csvContent], { type: 'text/csv' });
    const csvFile = new File([blob], file.data.fileName, { type: 'text/csv' });
    return await engine.ingestCSV(csvFile, options);
}
```

## 策略标签  
#逻辑重构 #回退机制缺失

---

# 2025-12-18 数据显示问题待分析

## 场景
CSV 重建成功但 VirtualDataGrid 仍显示元数据

## 问题描述
控制台日志：- ✅ 从 JSON 重建 CSV 成功，行数: 9773
- DuckDB ingest 成功
- 但数据表显示：fileName, fileType, fileSize 等元数据

## 初步分析
CSV 重建逻辑正常工作，问题可能在：

 1. VirtualDataGrid 使用错误的数据源
2. DuckDB queryChunk 返回错误数据
3. 数据传递链路中的映射错误

## 待验证
- DataViewer 如何传递 duckInfo 到 VirtualDataGrid
- VirtualDataGrid 如何查询和渲染数据
- 是否有数据源混淆？

## 策略标签
#待分析


## [2025-12-20] DataCleaner 布局重构失败与修复
- **关联文件**: `src/components/cleaning/DataCleaner.tsx`
- **问题描述**: 在尝试将数据表移至顶部并将建议与历史模块整合时，多次使用 `replace_file_content` 失败，报 "chunk 0: target content not found"。
- **根本原因**: 目标代码块逻辑较长，且包含多个嵌套结构，导致 `replace_file_content` 的目标匹配失效。
- **解决方法**: 改用 `write_to_file` 进行全文件重写，确保代码结构的完整性和布局调整的准确性。
- **验证结果**: 浏览器验证确认布局已成功切换：数据表位于顶部，建议与历史位于带标签页的底部面板。

---
*保持本日志更新，以便后续追溯。*
