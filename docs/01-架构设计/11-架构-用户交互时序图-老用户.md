# DataPrism 用户交互时序图 - 老用户缓存使用

**最后更新**: 2025-12-22  
**场景**: 老用户打开应用，直接从缓存加载项目的快速流程  
**架构版本**: v0.9.9（含AI预加载、缓存优化、智能降级）

```mermaid
sequenceDiagram
    actor 用户
    participant UI as LeftSidebar
    participant IDB as IndexedDB
    participant DB as DuckDBEngine
    participant Pyodide as Pyodide引擎
    participant Cache as 缓存管理器
    participant Nav as NavigationBar
    participant DC as DataCleaner
    participant Skills as Skills调度器
    participant AI as AI服务<br/>(API/本地)
    participant Exp as ExplorationFlow
    participant RG as ReportGenerator
    
    Note over 用户,RG: ═══════════ 阶段0: 快速启动 (已缓存) ═══════════
    
    用户->>UI: 访问应用 (二次及以后)
    activate UI
    
    par 并行初始化
        UI->>IDB: loadProjects()
        and
        UI->>Pyodide: 初始化Pyodide引擎
        
        alt 生产环境 (npm run build)
            Note over Pyodide: ⚡ 本地加载 (1-3秒)<br/>路径: dist/pyodide/<br/>离线: ✅ 无需网络<br/>CPU: 20-30%<br/>风扇: 🔇 静音
        else 开发环境 (npm run dev)
            Note over Pyodide: 🌐 CDN加载 (3-10秒)<br/>来源: jsdelivr CDN<br/>缓存: 浏览器缓存<br/>CPU: 40-60%<br/>网络: 需要连接
        end
        
        Pyodide->>Pyodide: 加载核心包 (pandas/numpy)
    end
    
    IDB-->>UI: 返回项目列表<br/>(含缓存元数据)
    UI->>UI: 自动选择第一个项目
    
    rect rgba(100, 200, 100, 0.3)
        Note over UI,DB: 🆕 智能缓存验证
        UI->>DB: 验证表 t_[缓存tableName]_working 是否存在
        
        alt 表仍有效 ✅
            DB-->>UI: ✅ 表存在
            UI->>Cache: 加载项目缓存<br/>(analysisCache, aiMode)
        else 表已失效 ❌
            DB-->>UI: ⚠️ Table does not exist
            Note over UI: 自动降级处理
            UI->>UI: markProjectAsStale()
            UI->>IDB: 更新 isStale=true
            UI-->>用户: 🟡 提示"数据已过期，请重新上传"
        end
    end
    
    Pyodide-->>UI: ✅ 引擎就绪
    UI-->>用户: ①直接显示缓存项目<br/>(数据表+历史)
    deactivate UI
    
    Note over 用户,RG: ═══════════ 阶段1: 数据清洗(缓存优先) ═══════════
    
    用户->>Nav: ②切换到"清洗"tab
    Nav->>DC: 渲染DataCleaner
    
    DC->>Cache: 检查 analysisCache.cleaning
    
    alt 有缓存 且 未过期
        Cache-->>DC: ✅ 返回缓存AI建议<br/>(18条清洗建议)
        DC-->>用户: ③**0秒显示**AI建议<br/>(无需等待)
    else 无缓存 或 isStale=true
        DC->>DC: 生成规则建议
        DC-->>用户: ③显示规则建议<br/>(后台加载AI...)
        
        rect rgba(100, 150, 255, 0.2)
            Note over DC,AI: 后台预加载 (用户无感)
            DC->>Skills: 请求AI建议(后台)
            
            alt API模式
                Skills->>AI: 调用DeepSeek API
            else 本地模型模式  
                Skills->>AI: 调用本地WebLLM
            end
            
            AI-->>Skills: 返回建议
            Skills->>IDB: 保存到 analysisCache.cleaning
            Skills-->>DC: 推送新建议
            DC-->>用户: 自动更新显示 ✅
        end
    end
    
    用户->>DC: ④选择建议并"应用"
    DC->>Skills: execute('sys_run_sql', {sql, permission:'CLEANING'})
    Skills->>DB: 执行清洗SQL
    DB-->>Skills: ✅ 清洗完成
    
    Skills->>IDB: 更新 analysisCache.insight.isStale=true
    Skills->>IDB: 更新 analysisCache.cleaning.isStale=true
    Skills-->>DC: ✅ 执行成功
    DC-->>用户: ⑤显示成功提示
    
    Note over 用户,RG: ═══════════ 阶段2: 洞察分析(智能刷新) ═══════════
    
    用户->>Nav: ⑥切换到"探索"tab
    Nav->>Exp: 渲染ExplorationFlow
    
    Exp->>Cache: 检查 analysisCache.insight
    
    alt 有缓存 且 未过期 (数据未变更)
        Cache-->>Exp: ✅ 返回缓存假设<br/>(3-5条假设+执行结果)
        Exp-->>用户: ⑦**0秒显示**假设卡片<br/>(含历史洞察)
        
        Note over Exp: 用户可直接查看<br/>或重新生成
    else 缓存过期 (isStale=true)
        Cache-->>Exp: ⚠️ 缓存已失效<br/>(数据已清洗)
        Exp->>Exp: 自动触发刷新
        Exp->>DB: 采样最新数据
        DB-->>Exp: 返回采样
        
        alt API模式
            Exp->>AI: 重新生成假设
        else 本地模式
            Exp->>AI: 本地生成假设
        end
        
        AI-->>Exp: 返回新假设
        Exp->>IDB: 更新 analysisCache.insight
        Exp-->>用户: ⑧显示更新后假设
    end
    
    用户->>Exp: ⑨点击假设卡片
    
    alt 已有执行结果 (缓存)
        Exp-->>用户: ⑩直接显示洞察<br/>(图表+分析)
    else 无执行结果
        Exp->>Skills: 请求生成代码+执行
        Skills->>AI: 生成Python代码
        AI-->>Skills: 返回代码
        Skills->>Pyodide: execute('sys_run_python')
        Pyodide-->>Skills: Base64图 + 摘要
        Skills-->>Exp: 洞察结果
        Exp-->>用户: ⑩显示洞察卡片
    end
    
    用户->>Exp: ⑪点击"采纳"
    Exp->>IDB: 保存到证据池
    IDB-->>Exp: ✅ 证据已采纳
    
    Note over 用户,RG: ═══════════ 阶段3: 报告生成(证据池) ═══════════
    
    用户->>Nav: ⑫切换到"报告"tab
    Nav->>RG: 渲染ReportGenerator
    RG->>IDB: 获取证据池数据
    IDB-->>RG: 返回已采纳证据<br/>(含历史采纳)
    
    rect rgba(200, 100, 200, 0.3)
        Note over RG: 🆕 增量报告生成
        RG->>RG: 检查上次报告版本
        alt 证据池无变化
            RG->>RG: 返回缓存报告HTML
        else 证据池已更新
            RG->>RG: 增量生成新章节
            RG->>RG: 合并报告内容
        end
    end
    
    RG-->>用户: ⑬显示完整报告预览
    
    用户->>RG: ⑭点击"导出HTML"
    RG->>RG: 生成单文件HTML<br/>(嵌入图表+代码)
    RG-->>用户: ⑮触发浏览器下载
```

---

## 老用户体验优势

### 1. **秒开应用** ⚡
- Pyodide本地缓存：1-3秒启动
- 项目列表即时加载
- 数据表快速验证

### 2. **智能缓存** 🧠
| 缓存项 | 缓存策略 | 失效条件 |
|-------|---------|---------|
| AI清洗建议 | localStorage | 数据清洗后 |
| 洞察假设 | IndexedDB | 数据变更后 |
| 执行结果 | IndexedDB | 代码变更后 |
| 证据池 | IndexedDB | 用户手动删除 |

### 3. **零等待显示** 🚀
- **清洗建议**: 缓存直接显示（0秒）
- **洞察假设**: 缓存直接显示（0秒）  
- **执行结果**: 缓存直接显示（0秒）
- **报告预览**: 增量生成（<1秒）

### 4. **自动降级** 🛡️
```
表失效 → 提示重新上传
API超时 → 降级到Mock
网络断开 → 本地模式继续
```

---

## 缓存失效机制

### 触发条件
1. **数据清洗后**:
   - `analysisCache.insight.isStale = true`
   - `analysisCache.cleaning.isStale = true`

2. **文件重新上传**:
   - 清空所有缓存
   - 重置 tableName

3. **用户手动刷新**:
   - 强制重新生成

### 验证逻辑
```typescript
// 1. 检查表是否存在
const tableExists = await db.checkTable(tableName);

// 2. 检查缓存是否有效
const isCacheValid = !analysisCache?.isStale && tableExists;

// 3. 决定加载策略
if (isCacheValid) {
    // 0秒显示缓存
    return loadFromCache();
} else {
    // 后台刷新
    return refreshInBackground();
}
```

---

## 性能对比

| 指标 | 新用户首次 | 老用户缓存 | 提升 |
|-----|----------|----------|------|
| **应用启动** | 1-3秒 | 1-3秒 | 相同 |
| **数据加载** | 2-5秒 | **0秒** | **即时** |
| **AI建议** | 10-30秒 | **0秒** | **即时** |
| **洞察显示** | 20-60秒 | **0秒** | **即时** |
| **报告生成** | 5-10秒 | **<1秒** | **10倍+** |

---

## 缓存优化策略

### 预加载机制 ⚡
```typescript
// 页面加载时并行预加载
Promise.all([
    loadPyodideEngine(),      // Pyodide引擎
    loadProjectMetadata(),    // 项目元数据
    preloadAICache(),         // AI缓存
    warmupSkillsDispatcher()  // Skills预热
]);
```

### 智能失效 🧹
- **时间维度**: 7天未使用 → 自动清理
- **空间维度**: 缓存超5MB → 压缩或删除旧项目
- **数据维度**: 数据变更 → 标记isStale，保留旧值供对比

---

## 用户体验亮点

1. **无感知更新**: 缓存显示 + 后台刷新
2. **智能提示**: 过期数据自动提示重新上传
3. **渐进增强**: 缓存失效不阻塞基础功能
4. **完整历史**: 证据池永久保留
5. **快速迭代**: 分析流程秒级响应
