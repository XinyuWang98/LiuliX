# DataPrism 用户交互时序图 - 新用户首次使用

**最后更新**: 2025-12-22  
**场景**: 新用户首次上传文件到生成报告的完整流程  
**架构版本**: v0.9.9（含Pyodide本地化、Generic Skills、敏感数据检测）

```mermaid
sequenceDiagram
    actor 用户
    participant UI as LeftSidebar
    participant IDB as IndexedDB
    participant Loader as useDataLoader
    participant DB as DuckDBEngine
    participant Pyodide as Pyodide引擎
    participant Detector as 敏感数据检测
    participant Nav as NavigationBar
    participant DC as DataCleaner
    participant Skills as Skills调度器
    participant AI as AI服务<br/>(API/本地)
    participant Exp as ExplorationFlow
    participant RG as ReportGenerator
    
    Note over 用户,RG: ═══════════ 阶段0: 引擎初始化 (首次) ═══════════
    
    用户->>UI: 访问应用 (首次)
    activate UI
    UI->>Pyodide: 初始化Pyodide引擎
    
    alt 生产环境 (npm run build)
        Note over Pyodide: ⚡ 本地加载 (1-3秒)<br/>路径: /pyodide/<br/>大小: 819MB (已打包)<br/>网络: 无需联网<br/>CPU: 20-30%峰值
        Pyodide->>Pyodide: 本地加载pandas/numpy/matplotlib
    else 开发环境 (npm run dev)
        Note over Pyodide: 🌐 CDN加载 (10-30秒)<br/>来源: cdn.jsdelivr.net<br/>大小: 819MB (动态下载)<br/>网络: 需要稳定连接<br/>CPU: 50-80%峰值
        Pyodide->>Pyodide: CDN动态下载依赖包
    end
    
    Pyodide-->>UI: ✅ 引擎就绪
    UI-->>用户: 显示空项目界面
    deactivate UI
    
    Note over 用户,RG: ═══════════ 阶段1: 文件上传与检测 ═══════════
    
    用户->>UI: ①点击"上传数据"
    UI->>用户: 显示文件选择器
    用户->>UI: ②选择CSV文件
    UI->>Loader: 触发文件解析
    Loader->>Loader: 解析CSV内容
    Loader->>DB: 创建双表<br/>(t_[timestamp]_original/working)
    DB-->>Loader: ✅ 表创建成功
    
    rect rgba(255, 200, 100, 0.3)
        Note over Loader,Detector: 🆕 敏感数据检测（企业版核心功能）
        Loader->>Detector: 检测敏感数据
        Detector->>Detector: Layer 1: 文件名检测
        Detector->>Detector: Layer 2: 列名检测
        Detector->>Detector: Layer 3: 内容采样检测<br/>(身份证/手机/邮箱)
        Detector->>Detector: 计算敏感度评分 (0-100)
        
        alt 评分 >= 60分 (高风险)
            Detector-->>用户: 🔴 弹窗提示<br/>"检测到敏感数据<br/>建议使用本地模型"
            用户->>UI: 选择: 本地模型/继续API
        else 评分 30-59分 (中风险)
            Detector-->>用户: 🟡 轻提示<br/>"可选本地模型"
        else 评分 < 30分 (低风险)
            Note over Detector: 无提示，默认API模式
        end
    end
    
    Loader->>IDB: 保存项目元数据<br/>(tableName, columns, aiMode)
    IDB-->>Loader: ✅ 持久化成功
    Loader-->>UI: 文件加载完成
    UI-->>用户: ③显示数据预览表格
    
    Note over 用户,RG: ═══════════ 阶段2: 数据清洗 (Skills架构) ═══════════
    
    用户->>Nav: ④切换到"清洗"tab
    Nav->>DC: 渲染DataCleaner组件
    DC->>DC: 生成规则建议<br/>(缺失值、重复行)
    DC-->>用户: ⑤显示规则建议
    
    用户->>DC: ⑥点击"AI清洗建议"
    DC->>Skills: 请求AI清洗建议
    
    alt API模式
        Skills->>AI: 调用DeepSeek API<br/>(清洗通道 30s)
        Note right of AI: 云端AI推理<br/>数据不上传<br/>仅Prompt+采样
    else 本地模型模式
        Skills->>AI: 调用本地WebLLM<br/>(Qwen2.5-1.5B)
        Note right of AI: 浏览器端推理<br/>完全离线<br/>数据不出本地
    end
    
    AI-->>Skills: 返回清洗建议
    Skills-->>DC: 转发建议列表
    DC-->>用户: ⑦显示增强建议
    
    用户->>DC: ⑧选择建议并"应用"
    DC->>Skills: execute('sys_run_sql', {sql, permission:'CLEANING'})
    
    rect rgba(100, 150, 255, 0.3)
        Note over Skills,DB: 🆕 Generic Skills架构（分离权限策略）
        Skills->>Skills: 权限检查<br/>(CLEANING模式)
        Skills->>Skills: SQL白名单验证<br/>✅ 允许: UPDATE/DELETE FROM/ALTER/CREATE<br/>❌ 禁止: DROP TABLE/TRUNCATE
        Note right of Skills: DELETE FROM 必须有WHERE条件<br/>防止误删全表数据
        Skills->>DB: 执行清洗SQL
        DB-->>Skills: ✅ 清洗完成
        Skills->>IDB: 记录清洗历史
        Skills->>IDB: 标记analysisCache.insight.isStale=true
    end
    
    Skills-->>DC: ✅ 执行成功
    DC-->>用户: ⑨显示成功提示
    
    Note over 用户,RG: ═══════════ 阶段3: 洞察分析 (Skills + Pyodide) ═══════════
    
    用户->>Nav: ⑩切换到"探索"tab
    Nav->>Exp: 渲染ExplorationFlow
    Exp->>Exp: 检测到无缓存假设
    
    alt API模式（成本考虑）
        Exp->>DB: 采样数据 (1000行)
        Note right of DB: Token成本限制<br/>严格控制样本量
    else 本地模式（适度放宽）
        Exp->>DB: 采样数据 (5000行)
        Note right of DB: 无Token成本<br/>小数据(<5k)全量<br/>大数据上限10k
    end
    
    DB-->>Exp: 返回采样数据
    
    alt API模式
        Exp->>AI: 生成假设<br/>(DeepSeek API, 120s)
    else 本地模型模式
        Exp->>AI: 生成假设<br/>(本地WebLLM)
    end
    
    AI-->>Exp: 返回3-5条假设
    Exp-->>用户: ⑪显示假设卡片
    
    用户->>Exp: ⑫点击假设卡片
    Exp->>Skills: 请求生成洞察代码
    
    alt API模式
        Skills->>AI: 生成Python代码
    else 本地模型模式
        Skills->>AI: 生成Python代码
    end
    
    AI-->>Skills: 返回Python代码
    Skills->>Skills: execute('sys_run_python', {code})
    
    rect rgba(150, 100, 255, 0.3)
        Note over Skills,Pyodide: 🆕 本地Python执行
        Skills->>Pyodide: runPython(code)
        Pyodide->>Pyodide: 加载df数据框
        Pyodide->>Pyodide: 执行分析代码
        Pyodide->>Pyodide: 生成matplotlib图表
        Pyodide-->>Skills: Base64图片 + 统计摘要
    end
    
    Skills-->>Exp: 洞察结果
    Exp-->>用户: ⑬显示洞察卡片<br/>(图表+分析)
    
    用户->>Exp: ⑭点击"采纳"
    Exp->>IDB: 保存到证据池
    IDB-->>Exp: ✅ 证据已采纳
    Exp-->>用户: ⑮显示成功提示
    
    Note over 用户,RG: ═══════════ 阶段4: 生成报告 ═══════════
    
    用户->>Nav: ⑯切换到"报告"tab
    Nav->>RG: 渲染ReportGenerator
    RG->>IDB: 获取证据池数据
    IDB-->>RG: 返回已采纳证据
    RG->>RG: 生成交互HTML报告<br/>(折叠/代码高亮/图表嵌入)
    RG-->>用户: ⑰显示报告预览
    
    用户->>RG: ⑱点击"导出HTML"
    RG->>RG: 生成单文件HTML
    RG-->>用户: ⑲触发浏览器下载
```

---

## 关键技术说明

### 1. **Pyodide本地化部署** ⚡
- **生产环境**: 从本地`/pyodide/`加载（1-3秒，10-50倍提升）
- **开发环境**: 从CDN加载（10-30秒，方便开发）
- **离线支持**: 完全支持断网使用

### 2. **敏感数据检测** 🔒
- **三层检测**: 文件名 + 列名 + 内容格式
- **智能评分**: 0-100分，自动判断风险等级
- **用户选择**: 高风险弹窗，中风险提示，低风险静默

### 3. **API vs 本地模型** 🎯
- **API模式**: DeepSeek云端推理（快速、准确）
- **本地模式**: WebLLM浏览器端（安全、离线）
- **统一接口**: Skills调度器对上层透明

### 4. **Generic Skills架构** 🛠️
- **sys_run_sql**: SQL执行（分离权限策略）
  - **READ_ONLY**（洞察分析）: 仅SELECT/DESCRIBE/SHOW
  - **CLEANING**（数据清洗）: 允许UPDATE/DELETE FROM/ALTER/CREATE，禁止DROP/TRUNCATE
  - **FULL**（系统内部）: 完全权限
- **sys_run_python**: Python执行（集成PyodideManager）
- **统一调度**: 日志、鉴权、错误处理一体化
- **安全机制**: DELETE FROM必须带WHERE条件

---

## 性能优化点

| 阶段 | 优化前 | 优化后 | 提升 |
|-----|-------|-------|------|
| Pyodide加载 | 10-30秒 | 1-3秒 | **10-50倍** |
| CPU占用 | 50-80% | 20-30% | **降低60%** |
| AI推理 | 仅云端 | 云端+本地 | **双轨可选** |
| Skills调用 | 50+函数 | <10函数 | **Token降80%** |

---

## 新用户体验亮点

1. **首次加载快**: Pyodide本地化，1-3秒启动
2. **数据安全提示**: 敏感检测自动提醒
3. **灵活选择**: API/本地模型自由切换
4. **全程离线**: 生产环境无需网络
5. **性能卓越**: CPU占用低，风扇静音
