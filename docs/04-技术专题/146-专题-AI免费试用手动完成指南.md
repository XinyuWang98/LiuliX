# AI免费试用功能手动完成指南

**状态**: 核心功能已完成75%，需手动完成UI集成和i18n翻译  
**预计时间**: 30-45分钟

---

## 📝 任务清单

- [ ] 任务1：修复导航栏徽章显示（**关键**，5分钟）
- [ ] 任务2：AI API调用添加headers（**关键**，15分钟）
- [ ] 任务3：添加i18n翻译（可选，15分钟）
- [ ] 任务4：测试验证（10分钟）

---

## 🔧 任务1：修复导航栏徽章显示

### 问题描述
`FreeTrialBadge` 组件已创建并导入，但未添加到DOM中

### 操作步骤

**文件**: `src/components/layout/NavigationBar.tsx`

**找到第50行**（后端状态指示灯的 `/>` 结束标签后）:
```tsx
                    animation: backendStatus === 'checking' ? 'pulse 1.5s infinite' : 'none',
                }}
            />

            <div style={{ width: '1px', height: '20px', background: 'var(--border)', margin: '0 var(--gap-xs)' }} />
```

**在 `/>` 和 `<div` 之间插入**:
```tsx
                    animation: backendStatus === 'checking' ? 'pulse 1.5s infinite' : 'none',
                }}
            />

            {/* 免费试用徽章 */}
            <FreeTrialBadge />

            <div style={{ width: '1px', height: '20px', background: 'var(--border)', margin: '0 var(--gap-xs)' }} />
```

**完成后**:
- 保存文件
- 刷新浏览器（徽章暂时不显示内容，因为还没有使用数据）

---

## 🔧 任务2：AI API调用添加headers

### 2.1 修改AIService（DeepSeek API）

**文件**: `src/services/aiService.ts`

#### 步骤A：导入用户ID管理器

在文件顶部添加导入:
```typescript
import { getUserId, getInviteCode } from '@/utils/userIdManager';
```

#### 步骤B：修改DeepSeek API调用

**找到清洗建议API调用**（搜索 `deepseek-cleaning`）:
```typescript
const response = await fetch('http://localhost:3001/api/proxy/deepseek-cleaning', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
        'x-api-key': apiKey || 'default',
    },
    body: JSON.stringify({ data }),
});
```

**修改为**:
```typescript
const response = await fetch('http://localhost:3001/api/proxy/deepseek-cleaning', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
        'x-api-key': apiKey || 'default',
        'x-user-id': getUserId(),                    // ✅ 新增
        'x-invite-code': getInviteCode() || '',      // ✅ 新增
    },
    body: JSON.stringify({ data }),
});
```

**找到洞察建议API调用**（搜索 `deepseek-insight`）:
```typescript
const response = await fetch('http://localhost:3001/api/proxy/deepseek-insight', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
        'x-api-key': apiKey || 'default',
    },
    body: JSON.stringify({ data }),
});
```

**修改为**:
```typescript
const response = await fetch('http://localhost:3001/api/proxy/deepseek-insight', {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
        'x-api-key': apiKey || 'default',
        'x-user-id': getUserId(),                    // ✅ 新增
        'x-invite-code': getInviteCode() || '',      // ✅ 新增
    },
    body: JSON.stringify({ data }),
});
```

#### 步骤C：处理429错误和更新使用次数

在每个API调用的 `response` 处理中添加:
```typescript
const result = await response.json();

// ✅ 新增：处理超限错误
if (response.status === 429) {
    throw new Error(result.message || '免费试用次数已用完');
}

// ✅ 新增：更新本地使用次数
if (result.usage) {
    localStorage.setItem('free_trial_usage', JSON.stringify(result.usage));
    window.dispatchEvent(new Event('free-trial-update'));
}

return result;
```

---

### 2.2 修改LocalLLMService（本地模型）

**文件**: `src/services/localLLMService.ts`

#### 步骤A：导入用户ID管理器

在文件顶部添加:
```typescript
import { getUserId, getInviteCode } from '@/utils/userIdManager';
```

#### 步骤B：修改generateInsight方法

**找到** `generateInsight` 方法中的fetch调用（约Line 121）:
```typescript
const response = await fetch(`${API_BASE_URL}/generate`, {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify({
        prompt,
        maxTokens: 2048,
        temperature: 0.7
    })
});
```

**修改为**:
```typescript
const response = await fetch(`${API_BASE_URL}/generate`, {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json',
        'x-user-id': getUserId(),                    // ✅ 新增
        'x-invite-code': getInviteCode() || '',      // ✅ 新增
    },
    body: JSON.stringify({
        prompt,
        maxTokens: 2048,
        temperature: 0.7,
        type: 'insight'  // ✅ 新增：默认为insight，清洗时需要传'cleaning'
    })
});
```

**添加错误处理和使用次数更新**:
```typescript
const result = await response.json();

// ✅ 新增：处理超限错误
if (response.status === 429) {
    throw new Error(result.message || '免费试用次数已用完');
}

// ✅ 新增：更新本地使用次数
if (result.usage) {
    localStorage.setItem('free_trial_usage', JSON.stringify(result.usage));
    window.dispatchEvent(new Event('free-trial-update'));
}

return result.text;
```

---

## 🔧 任务3：添加i18n翻译（可选）

### 步骤A：找到i18n类型定义

**文件**: `src/types/i18n.ts`

**在 `header` 部分添加**（约Line 36-45）:
```typescript
// 导航栏
nav: {
    appName: string;
    noProject: string;
    dashboard: string;
    promptLibrary: string;
    settings: string;
    user: string;
    theme: string;
};

// ✅ 新增：导航栏header
header: {
    freeTrial: string;
    inviteCodeTrial: string;
};
```

**在文件末尾添加 `inviteCode` 部分**:
```typescript
// ✅ 新增：邀请码
inviteCode: {
    title: string;
    placeholder: string;
    activate: string;
    emptyError: string;
    invalidError: string;
    networkError: string;
    hint: string;
};
```

**在 `common` 部分添加**:
```typescript
common: {
    loading: string;
    initializing: string;
    error: string;
    success: string;
    cancel: string;
    confirm: string;
    verifying: string;  // ✅ 新增
    // ...
};
```

### 步骤B：添加中文翻译

**文件**: `src/locales/zh-CN.ts`（或对应的中文翻译文件）

**找到 `header` 或 `nav` 附近，添加**:
```typescript
header: {
    freeTrial: '免费试用',
    inviteCodeTrial: '邀请码额度',
},

inviteCode: {
    title: '输入邀请码',
    placeholder: '请输入邀请码',
    activate: '激活',
    emptyError: '请输入邀请码',
    invalidError: '邀请码无效或已过期',
    networkError: '网络错误，请稍后重试',
    hint: '输入邀请码可获得20次AI使用额度',
},

common: {
    //...
    verifying: '验证中...',
},
```

### 步骤C：添加英文翻译

**文件**: `src/locales/en-US.ts`

```typescript
header: {
    freeTrial: 'Free Trial',
    inviteCodeTrial: 'Invite Quota',
},

inviteCode: {
    title: 'Enter Invite Code',
    placeholder: 'Enter your invite code',
    activate: 'Activate',
    emptyError: 'Please enter invite code',
    invalidError: 'Invalid or expired invite code',
    networkError: 'Network error, please try again',
    hint: 'Enter invite code to get 20 AI uses',
},

common: {
    //...
    verifying: 'Verifying...',
},
```

---

## 🧪 任务4：测试验证

### 4.1 浏览器控制台测试

**在浏览器控制台执行**:
```javascript
// 1. 设置测试数据（免费用户）
localStorage.setItem('free_trial_usage', JSON.stringify({
    type: 'free',
    cleaning: 2,
    insight: 3
}));
window.dispatchEvent(new Event('free-trial-update'));

// 2. 检查徽章是否显示 "5/10"
// 应该看到导航栏右侧显示 "🎁 免费试用 5/10"

// 3. 切换为邀请码用户测试
localStorage.setItem('liulix_invite_code', 'LIULIX2025');
localStorage.setItem('free_trial_usage', JSON.stringify({
    type: 'invite',
    total: 15
}));
window.dispatchEvent(new Event('free-trial-update'));

// 4. 检查徽章是否显示 "15/20" 且为金色
```

### 4.2 功能测试

1. **测试AI调用计数**:
   - 触发一次清洗建议
   - 打开浏览器Network面板
   - 检查请求headers中是否有 `x-user-id` 和 `x-invite-code`
   - 检查响应是否返回 `usage` 字段
   - 徽章数字是否增加

2. **测试超限提示**:
   ```javascript
   // 手动设置为已超限
   localStorage.setItem('free_trial_usage', JSON.stringify({
       type: 'free',
       cleaning: 5,
       insight: 5
   }));
   ```
   - 再次触发AI调用
   - 应该收到429错误和友好提示

3. **测试邀请码验证**:
   - 点击徽章（如果已实现onClick）或直接访问 `/api/validate-invite-code`
   - 输入测试邀请码：`LIULIX2025`
   - 验证成功后徽章应该变为金色

---

## 🚀 部署前检查清单

- [ ] 后端 `.env.local` 已配置：
  ```bash
  ENABLE_FREE_TRIAL_LIMIT=false  # 测试阶段保持false
  FREE_TRIAL_CLEANING_LIMIT=5
  FREE_TRIAL_INSIGHT_LIMIT=5
  INVITE_CODE_TOTAL_LIMIT=20
  VALID_INVITE_CODES=LIULIX2025,BETA001  # 修改为真实邀请码
  ```
- [ ] 所有API调用已添加 `x-user-id` 和 `x-invite-code` headers
- [ ] 导航栏徽章正常显示
- [ ] 超限提示正常工作
- [ ] i18n翻译完整（中英文）
- [ ] 后端日志显示"免费试用限制: 关闭（测试模式）"

---

## 📚 相关文档

- 策略文档：`docs/04-技术专题/45-设计-AI免费试用与邀请码策略.md`
- 项目概览：`docs/00-必读/01-核心-项目概览与现状.md`

---

**完成后请告知，我可以协助验证和troubleshooting！**
