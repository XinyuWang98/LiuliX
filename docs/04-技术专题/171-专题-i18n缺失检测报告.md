# i18n 多语言缺失检测报告

**生成时间**: 2026-01-12 22:20
**检测脚本**: `scripts/check-i18n.ts`
**包含范围**: `src` 下所有 `.ts`, `.tsx` 文件

---

## 1. 检测结果概览
> **检测时间**: 2026-02-14 22:33:06
> **状态**: ✅ 已全部修复 (All Fixed)
> **统计**:
> - zh-CN 缺失: 0 (原 28)
> - en-US 缺失: 0 (原 46)

## 2. 修复日志
已完成所有缺失 Key 的补充，并修正了代码中的引用错误（如 `report.report.title` -> `report.title`）。
检测脚本 `scripts/check-i18n.ts` 现在报告 "🎉 完美！未发现缺失的翻译。"。

---
**以下为原始缺失记录（归档）：**`src` 下所有 `.ts`, `.tsx` 文件

---

## ⚠️ 检测结果总览

| 语言        | 缺失 Key 数量 | 状态     |
| :---------- | :------------ | :------- |
| **🇨🇳 zh-CN** | **28**        | ❌ 需修复 |
| **🇺🇸 en-US** | **46**        | ❌ 需修复 |

---

## 🔍 详细缺失清单

### 1. 🇨🇳 zh-CN (简体中文) 缺失

**Prompt 相关**
- `prompt.card.official` (PromptDetailModal.tsx)
- `prompt.detail.info` (PromptDetailModal.tsx)
- `prompt.library.noPromptsFound` (PromptLibrary.tsx)

**数据清洗 (Cleaning)**
- `cleaning.moreFiles`
- `common.avgLength`
- `common.format`
- `common.high`, `common.medium`, `common.low` (CleaningSuggestions)
- `common.median`, `common.range`, `common.sample`
- `common.sensitive`

**数据源与项目**
- `dataSource.connectDatabase`
- `dataSource.createProject`
- `dataSource.project.noProjects`
- `dataSource.project.recentProjects`
- `dataSource.project.selectProject`

**报告与通用**
- `report.hideNotebook`
- `report.report.title`
- `report.showNotebook`
- `report.status.noRecordsHint`
- `report.useNewWorkbench`
- `common.expand`
- `common.noData`
- `data.columns`, `data.rows`

**洞察**
- `insightChain.ignored`

---

### 2. 🇺🇸 en-US (English) 缺失

> 除上述 zh-CN 缺失项外，en-US 还额外缺失以下内容：

**清洗建议**
- `cleaning.defaultReason`
- `cleaning.ignore`
- `cleaning.requirements`
- `common.column`, `common.type`
- `common.none`
- `common.table`
- `common.totalColumns`, `common.totalRows`
- `common.rename`

**欢迎页 (Welcome)**
- `welcome.prefix`
- `welcome.subtitle`
- `welcome.feature1`, `welcome.feature2`, `welcome.feature3`
- `welcome.uploadButton`

**Prompt 与其他**
- `prompt.library.card.usageTooltip`
- `workshop.dataExploration`

---

## 🛠️ 下一步建议

1. **[P0] 修复通用词汇 (`common.*`)**: 这些词汇被大量清洗建议 Prompt 引用，缺失会导致生成的建议描述不完整。
2. **[P0] 修复 UI 显性缺失**: `PromptDetailModal` 和 `Welcome` 页面的文案缺失直接影响用户体验。
3. **工具集成**: 建议将 `npx tsx scripts/check-i18n.ts` 加入 Git pre-commit hook 或 CI 流程。
