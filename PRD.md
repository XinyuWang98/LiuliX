
# 📑 DataPrism 产品需求文档 (PRD) - 最终版（完整版）

## 0\. AI 实现前置指令 (AI Studio Directive)

**目标：** 基于以下 PRD，使用 **React (TypeScript)** 和 **Web Workers** 构建纯前端的 DataPrism 应用。

### 0.1 核心技术栈与约束

1.  **前端框架：** 必须使用 **React** 和 **TypeScript**。
2.  **数据处理：** 必须通过 **Pyodide/WebAssembly (Wasm)** 运行 **Pandas/Numpy** 代码，所有计算密集型任务必须隔离在 **Web Workers** 中运行。
3.  **持久化：** 用户数据和历史记录必须存储在 **IndexedDB** 中，绝不允许向外部服务器传输用户数据。

### 0.2 核心数据结构定义

为确保 AI 准确实现，以下是 PRD 中关键对象的 TypeScript 接口定义：

#### A. Prompt 库结构 (`PromptSchema`)

该接口定义了 AI 分析提示词的配置格式。

```typescript
interface PromptSchema {
    id: string; // 唯一标识符，如 'cleaning_iqr'
    title: string; // 提示词的中文标题
    category: '清洗' | '分析' | '报告' | '特征工程';
    prerequisite: { [key: string]: any }; // 上下文依赖条件 (如 {has_date_type: true})
    full_prompt: string; // AI 最终执行的指令文本模板 (包含变量占位符)
    input_vars: string[]; // 需要用户选择或程序注入的变量列表 (如 ['{target_column}', '{project.alpha_value}']) 【补充：支持项目全局变量引用】
    output_format: 'Python Code + Chart' | 'Python Code' | 'Summary' | 'Model Report';
}
```

#### B. 数据列元数据结构 (`ColumnMetadata`)

该接口用于缓存数据列的统计信息，支撑 Prompt 智能匹配。

```typescript
interface ColumnMetadata {
    column_name: string;
    current_type: 'String' | 'Numeric' | 'Date' | 'Categorical'; // 可被用户手动修正
    unique_count: number;
    missing_count: number;
    missing_ratio: number;
    stats_numeric?: { min: number; q1: number; median: number; q3: number; max: number; std_dev: number; variance: number };
    stats_categorical?: { top_5_values: Array<{ value: string; count: number }> };
}
```

#### C. 结构化假设 Schema (`HypothesisSchema`)

该接口定义了 AI 生成或用户输入的假设的结构化解析结果。

```typescript
interface HypothesisSchema {
    hypothesis_text: string; // AI 或用户提供的中文假设描述。
    test_type: string; // 假设检验的类型（例如：均值差异检验、相关性检验）。
    required_fields: string[]; // 验证假设所需的关键字段名或字段类型。
    validation_analysis: string; // 验证该假设所需的具体分析方法（如：T-Test, ANOVA, Chi-Square）。
    suggested_prompt_id: string; // 推荐用于验证该假设的 Prompt 库 ID。
}
```

#### D. 配色方案结构 (`ThemeSchema`)

该接口定义了一套完整的 UI 配色方案。

```typescript
interface ThemeSchema {
    id: string; // 唯一标识符，如 'neufuture'
    title: string; // 配色方案的中文标题 (界面展示)
    is_dark: boolean; // 是否为深色主题 (True/False)
    colors: {
        '--color-background-primary': string; // 主背景色（如 #292d32）
        '--color-background-secondary': string; // 次级背景色/卡片背景色
        '--color-text-primary': string; // 主要文字颜色
        '--color-text-secondary': string; // 次要文字颜色
        '--color-accent-primary': string; // 主要强调色/选中色
        '--color-accent-secondary': string; // 次要强调色/图表色系基准色
        '--color-shadow-light': string; // 新拟态 - 浅阴影色
        '--color-shadow-dark': string; // 新拟态 - 深阴影色
        // ... (可按需添加其他关键颜色变量)
    };
}
```

#### E. 特征转换记录结构 (TransformationSchema)

```typescript
interface TransformationSchema {
    column_name: string;
    transformation_type: string; // 如 'Z-Score Normalization', 'IQR Outlier Capping'
    parameters: { [key: string]: any }; // 具体的参数值 (如 {mean: 100, std: 15} 或 {lower_bound: 10, upper_bound: 90})
    source_prompt_id: string; // 引用生成该转换的 Prompt ID
    is_active: boolean; // 是否应用于当前数据帧
}
```

#### F. 分析管道结构 (`PipelineSchema`)

该接口定义了用户当前项目中的分析流程节点序列。

```typescript
interface PipelineSchema {
    node_id: string; // 唯一节点ID
    node_type: 'Flow' | 'Transform' | 'Analysis' | 'Report'; // 节点类型
    prompt_ref_id?: string; // 如果是 Transform 或 Analysis，引用 PromptSchema ID
    input_vars: { [key: string]: any }; // 存储节点的输入参数和配置
    order: number; // 节点在流程中的顺序
    status: 'completed' | 'pending' | 'error'; // 当前执行状态
}
```

**新增强制约束（Grok 补）**：

  - 所有 AI 输出（洞察、报告、摘要）必须默认强制中文。如果检测到英文，前端必须自动走本地轻量翻译 Wasm（如 transformers.js 小模型）进行二次翻译，绝不让英文污染最终报告。

-----

## 1\. 产品定位

**DataPrism** 是一款 Future Tech 风格的 AI 主导的 EDA (Exploratory Data Analysis) 产品。
核心理念：以 AI 主导的数据分析流程实现完美的闭环，为分析师和数据科学家提供高效率、高智商的工作体验。

-----

## 2\. 核心模块与功能

### 2.1 上方导航栏 (Navigation Bar)

  * **主题切换器（新增核心功能）：**

      * 在**导航栏**的右侧，靠近“设置”和“用户头像”的位置，新增一个**主题切换按钮（如月亮/太阳图标）**。
      * 点击按钮，必须弹出一个下拉菜单或 Modal，允许用户从预设的**主题列表**中选择（例如：`Neufuture 暗夜版`, `高对比度专业版`, `极简亮白版`）。
      * **持久化：** 用户选择的主题必须被**本地持久化**（存储在 `localStorage` 或 `IndexedDB` 中），确保应用重新加载后保持一致。

  * **AI 分析 Prompt 库（结构化与独立维护）**：
    此模块是 DataPrism 区别于传统分析工具的核心功能，用于指导 AI 执行复杂的分析任务。

      * **维护要求：** Prompt 库的内容**必须从代码中分离**，以 **独立的 JSON 配置文件** 或 **在线表格** 的形式维护，前端应用在启动时读取该配置。
      * **Prompt 对象结构（Schema）：** 必须遵循上述 **`PromptSchema`** 接口定义。

| 字段名称 | 目的 | 示例 |
| :--- | :--- | :--- |
| **`id`** | 唯一标识符，用于证据池引用。 | `cleaning_iqr` |
| **`title`** | 提示词的中文标题（界面展示）。 | **标准化异常值处理** |
| **`category`** | 分类（`清洗`, `分析`, `报告`）。 | `清洗` |
| **`prerequisite`** | **上下文依赖条件（智能匹配核心）。** 用于前端判断何时高亮显示该 Prompt（例如：`{has_date_type: true}`）。 | |
| **`full_prompt`** | AI 最终执行的完整指令文本。 | `"请对 {target_column} 执行 T-test..."` |
| **`input_vars`** | 提示词中需要用户提供或程序注入的**变量**（如：`{target_column}`, `{alpha_value}`）。 | `[{target_column}, {group_column}]` |
| **`output_format`** | 期望 AI 输出的格式。 | `Python Code + Chart` |

  * **预设 Prompt 示例：** (作为初始配置，必须遵循上述 Schema，为 AI 提供高质量的分析流程范例。)

| 类型 | 预设 Prompt 示例 (中文) | 目的 |
| :--- | :--- | :--- |
| **I. 清洗/预处理** | **标准化异常值处理** | 保证数据质量，规范清洗流程。 |
| **I. 清洗/预处理** | **文本特征工程** | 快速进行文本数据的准备和探索。 |
| **II. 核心分析/洞察** | **验证零假设** | 引导心流，将用户注意力引向假设验证。 |
| **II. 核心分析/洞察** | **客户分段建模 (RFM)** | 引导用户进行商业价值较高的复杂分析。 |
| **II. 核心分析/洞察** | **时间序列趋势分析** | 确保对时间维度数据的专业处理。 |
| **III. 特征工程** | **Z-Score 标准化** | 建模前的必备步骤，对数值字段进行预处理。 |

  * **技术与交互要求 (Prompt 库)：**

      * **策略触发（增强）：** 预设 Prompt 必须支持 **“上下文智能匹配”**。只有当数据满足 `prerequisite` 条件时，Prompt 才能被高亮显示和选择。
      * **变量提示：** 当用户选择带有 `input_vars` 的 Prompt 时，界面必须弹出对话框提示用户选择/输入对应的变量（如列名、阈值）。
      * **用户维护：** 必须提供完整的 **CRUD** (创建、读取、更新、删除) 界面。
      * **分享与导入（预留功能）：** 预留未来支持用户或团队之间导入/导出/分享自定义 Prompt 文件的接口。
      * **项目设置 统计参数（新增）：** 增加一个入口，配置并存储项目的**全局 $\alpha$ 值**和**默认抽样比例**。

  * **基础功能**：设置、用户头像、项目名称显示（可编辑）。

-----

### 2.2 左侧：数据源 (Data Sources)

  * **文件导入（增强）：** 
      * **本地文件支持**：支持 **CSV/XLSX**。支持多文件一次上传。**新增支持**：**JSON, Parquet, HDF5** 格式。预留 **SQL/API 连接** 接口。
      * **Google Sheets 集成（新增核心功能）**：
          * 支持通过 **Google Sheets URL** 直接导入数据（使用 Google Sheets API v4）。
          * **认证机制**：使用 OAuth 2.0 授权流程，支持读取用户有权访问的任何 Sheets。
          * **公开 Sheets**：对于公开分享的 Sheets，应提供免登录快速导入选项。
          * **数据规模支持**：必须达到 **Google Sheets 级别的处理能力**（单表 **500 万单元格** 或 **100 万行 × 5 列**）。
          * **实时同步（可选预留）**：预留接口，未来支持 Sheets 数据变更自动同步到 DataPrism。
          * **UI 交互**：
              * 左侧边栏新增 "从 Google Sheets 导入" 按钮。
              * 点击后弹出对话框，要求用户输入 Sheets URL 或文件 ID。
              * 显示导入进度（读取行数/总行数）。
              * 导入完成后，Sheets 数据应与本地文件一样被纳入项目管理。
      * **大文件处理能力提升（核心性能要求）**：
          * **行数上限提升**：从当前 50,000 行提升至 **100 万行**（抽样模式）。
          * **流式解析**：对超大 CSV 文件（>100MB），使用流式解析技术（Papa Parse Streaming Mode）。
          * **分块加载**：实现按需分块加载数据（每块 10,000 行），避免一次性加载全部数据到内存。
          * **虚拟滚动优化**：数据表必须使用虚拟滚动（react-window），仅渲染可视区域的行。
          * **IndexedDB 优化**：优化存储策略，支持存储更大规模的数据集。
  * **智能命名**：导入时 AI 自动读取内容，给出智能命名（用户可采纳或自定义）。
  * **项目列表**：
      * 支持多文件导入到同一项目，预留对多文件进行 **JOIN/合并** 的架构。
      * 支持项目置顶、重命名、一键收起/展开。

-----

### 2.3 中间：数据探索 (Data Exploration)

**核心交互区**，所有功能严格遵循执行流程（线性展开）：

  * **线性流程进度条（更新）：** 界面顶部必须向用户清晰展示当前分析流程的进度和所处阶段（例如：**探索/清洗 $\rightarrow$ 假设 $\rightarrow$ 分析**）。
  * **回溯操作：** 允许用户点击进度条上的**上一个流程节点**，进行回溯操作，回到上一个主要阶段（例如：从“假设”回溯到“探索/清洗”）。
  * **核心节点：** 流程应由可拖拽的独立节点组成，包括：数据探索/清洗 $\rightarrow$ 特征工程/转换 $\rightarrow$ 分析假设/验证 $\rightarrow$ 报告生成。
<!-- end list -->

1.  **数据编辑器 (Data Editor) & 清洗（前置流程）**：

      * 默认展示第一个文件。
      * **Kaggle 风格展示**：数据表 + 统计信息 + 数据分布图融合。其中统计信息、数据分布图需要以微型图的形式集成在数据表的标题下方，支持用户一键展开/收起统计信息卡片。
      * **数据类型修正（新增）：** **数据表列头必须允许用户手动修正**列的数据类型（String, Numeric, Date, Categorical）。修正后必须更新 `ColumnMetadata`。
      * **详细统计信息要求：** 必须基于 `ColumnMetadata` 接口展示统计信息。在统计信息展示部分，新增一个“特征”标签页，专门展示和管理遵循 TransformationSchema 的新生成特征及其参数

| 数据类型 | 统计指标 (Metrics) | 展示要求 |
| :--- | :--- | :--- |
| **数值字段 (Numeric)** | **MIN, Q1, MEDIAN, Q3, MAX** | 核心五数概括数据分布。 |
| | **标准差 (Std Dev), 方差 (Variance)** | 衡量数据的离散程度和波动性。 |
| **文本/日期字段 (Categorical/Date)** | **TOP 5 VALUE (按数量降序)** | 快速识别字段内最常见的 5 个值及其频数。 |
| **所有字段 (All Types)** | **唯一值 (Unique Count), 缺失值 (Missing Count)** | 快速评估数据质量。 |

  * **数据质量识别：** 缺失值 (Missing Count) 必须识别并计数以下所有场景：**NaN**、纯连字符 (**-**)、字符串 **"NULL"**、以及**空字符串** (**""**)。

  * **数据展示优化：** 每行统计信息最多展示 **2 个关键指标**。所有过长的数值必须使用**专业简写**（如 `1M`、`100K`）。

  * **空值可视化（补充）：** 数据表必须对空值（NaN、NULL 等）单元格提供**可视化标记**（例如：用浅灰色条块填充单元格），而非仅依赖缺失值计数。

  * **大文件处理与抽样提示（关键交互）：**

      * **触发条件：** 当文件行数**超过 50,000 行**或文件大小**超过 50MB** 时，系统应立即弹出**模态提示框**。
      * **提示内容与选项：** 提示用户数据集较大可能影响性能，推荐用户采用 **“立即抽样”** 或 **“替换文件”** 选项，并提供 **“强制导入”** 选项。**【抽样比例配置：** 允许用户在 **5%** 到 **20%** 之间选择抽样比例，默认为 10%。**】**
      * **抽样标识：** 抽样后的数据编辑器界面顶部必须显示 **“当前数据为抽样子集（N 行）”** 的**醒目标识**。

  * **AI 清洗与版本控制（增强）**：

      * 勾选 AI 建议的清洗方式一键应用。
      * **缺失值高级填充（新增）：** 提供 **ffill (向前填充)**、**bfill (向后填充)** 和 **指定常量填充** 的快捷操作。
      * **数据抽样（新增）：** 提供**随机抽样**和**分层抽样** Prompt 或按钮。
      * **版本回溯：** 实现基于 **操作指令序列** 的历史记录/回滚机制。

  * **流程**：确认清洗方案 $\rightarrow$ 跳出完成提示 $\rightarrow$ **进入下一阶段（假设建议）**。

<!-- end list -->

2.  **分析假设建议（结构化增强 - 调整后第二阶段）**：

      * **AI 初始生成：** AI 必须基于**清洗后的数据和元数据**，生成遵循 **`HypothesisSchema`** 结构的对象数组。
      * **用户输入与结构化：**
          * 必须提供一个输入框，允许用户自由输入**自定义的分析假设**。
          * **AI 拆解要求：** 系统立即调用 LLM，根据**当前干净数据集**的元数据，将用户的假设拆解为遵循上述 `HypothesisSchema` 的结构化对象。
          * **用户确认：** 拆解完成后，界面应向用户清晰展示 AI 确定的 **检验类型、所需字段** 和 **验证方法**，允许用户进行微调或确认。
      * **交互**：用户确认的假设默认置顶，后续分析围绕此展开，并实现从假设到 **Prompt 库的一键推荐跳转**（基于 `suggested_prompt_id`）。

3.  **分析看板 (Analysis Dashboard)**：

      * **AI 洞察**：自动遍历数据，给出洞察和图表。
      * **代码生成**：用户通过指令/Prompt，让 AI 生成 **Python 代码** + 运行结果（图表）。
      * **代码沙盒反馈（补充）：** Python 代码执行失败时，前端必须捕获 **Python 的 Traceback 信息**，并在代码单元格下方以**可折叠**的方式展示，并提供**AI 修复**按钮。
      * **模型实验（新增）：** 预留或添加 **“模型实验 (Model Experiment)”** 阶段，支持快速训练基础模型。
      * **模型 Prompt：** 预设 Prompt 库必须包含 **“快速线性回归”** 和要求输出 **模型性能指标**（如 $R^2$, RMSE, AUC 等）的策略。

4.  **项目分析报告 (Project Report)**：

      * **证据整合**：用户可将上述模块的任意区域加入“证据池”。
      * **报告生成**：AI 基于假设 + 证据，以 Notebook 形式（单元格）生成报告。

<!-- end list -->

  * **证据池交互（原只有展示，现补充为可操作）**：

      * 支持拖拽调整证据顺序
      * 支持一键删除、一键标注“已手动修改”
      * 报告页左侧出现“插入证据”按钮，点开弹出证据池列表，多选后直接插入光标位置

  * **报告页必须支持可编辑模式（核心！）**：

      * 报告采用 Notion-style 可编辑单元格
      * 用户修改后，对应证据池项自动标黄「已手动修改」+ 时间戳
      * **锁定机制（补充）：** 用户编辑过的报告单元格应自动被标记为**锁定**。后续 AI 生成报告内容时，必须**跳过**所有锁定单元格。
      * 提供「锁定报告」按钮，锁定后不可再编辑，视为最终签字

### 2.4 右侧：智能工坊 (Smart Workshop)

  * **上方：AI 交互功能集成** (按钮)
  * **下方：证据池 (Evidence Pool)**：
      * 展示用户勾选保存的内容。
      * **持久化（增强）：** 使用 **IndexedDB** 确保证据池和 Chatbot 历史记录在浏览器关闭后**不丢失**。应支持添加 **模型评估图表** 和 **性能报告**。
      * 使用 Modal + 可编辑表格形式
      * 支持批量导入 JSON
      * 每条 Prompt 右侧必须有「测试」按钮 $\rightarrow$ 弹出变量填写框 $\rightarrow$ 实时看 AI 输出效果（必须有）

-----

## 3\. UI/UX 设计要求 (Future Tech Style)

  * **风格**：柔和的新拟态 (Neumorphism) 风格，背景使用低饱和度的深灰蓝 (\#292d32) ，界面元素要求内凹、立体、具有柔和光影，营造轻量且精致的触感。
  * **配色方案管理：** 必须遵循 **4.1 全局样式变量系统** 的规范，所有颜色、间距、字体、圆角和动效，都通过 CSS 变量进行统一管理。

-----

## 4\. 非功能性要求 (NFR)

### 4.1 全局样式变量系统（核心新增 - 全变量化，永不硬编码）

所有 UI 元素（颜色、间距、字体、圆角、阴影、动画、图标、进度条）必须通过 **CSS 自定义属性（:root 变量）** 统一管理。

  * **变量命名规范：** 遵循 `  --类别-子类-级别 ` 的规范（例如：`--font-base-sm`、`--btn-radius`）。
  * **强制约束：** 前端生成代码时，**不得出现任何硬编码**的像素值、颜色值、秒数（例如 `margin: 16px`、`color: #fff`、`transition: 0.3s` 必须全改变量）。
  * **目的：** 实现一键换肤、调整间距、改变用户手感，并预留后期用户可自定义主题 JSON 上传的接口。
  * **切换机制：** 设置页增加主题切换下拉（默认：暗夜酷炫；可选：未来极光、黑洞模式）。运行时通过动态替换根 CSS 变量值来实现。
  * **验收标准：** 修改 `  --gap-m ` 的值后，三栏间距必须全局同步刷新；修改 `  --btn-radius ` 后，所有按钮圆角必须同步变动；修改 `  --fs-base ` 后，正文字体大小必须全屏更新。

#### 4.1.1 颜色变量（配色方案）

| 变量名称 | 目的 | 示例值 |
| :--- | :--- | :--- |
| `  --bg-main ` | 主背景，黑洞底 | `\#1C1C1E` |
| `  --bg-panel ` | 面板色，深蓝灰 | `\#2C2C2E` |
| `  --bg-accent ` | 高亮/按钮主色，霓虹青 | `\#007AFF` |
| `  --text-primary ` | 主文字，白 | `\#FFFFFF` |
| `  --text-secondary ` | 副文字，灰 | `\#A0A0A0` |
| `  --warning ` | 警告/红，bug色 | `\#FF453A` |
| `  --hover-bg ` | 悬浮背景，青透 | `rgba(0, 122, 255, 0.1)` |
| `  --border ` | 边框线，暗蓝 | `\#4A4A4C` |

#### 4.1.2 间距系统（gap & padding）

| 变量名称 | 目的 | 示例值 |
| :--- | :--- | :--- |
| `  --gap-xs ` | 单元格间隙 | `4px` |
| `  --gap-s ` | 小间隙，图标旁 | `8px` |
| `  --gap-m ` | 标准间隙，三栏/模块间 | `16px` |
| `  --gap-l ` | 大间隙，页边距 | `24px` |
| `  --padding-card ` | 卡片内边距 | `16px` |
| `  --padding-btn ` | 按钮内边距 | `12px 24px` |

#### 4.1.3 字体层级（fs = font-size）

| 变量名称 | 目的 | 示例值 |
| :--- | :--- | :--- |
| `  --fs-xs ` | 提示/脚注 | `12px` |
| `  --fs-sm ` | 标签/按钮文字 | `14px` |
| `  --fs-base ` | 正文，默认 | `16px` |
| `  --fs-lg ` | 小标题 | `18px` |
| `  --fs-xl ` | 大标题/假设卡 | `24px` |
| `  --line-height ` | 行高，全局统一 | `1.5` |

#### 4.1.4 按钮样式（btn 专用）

| 变量名称 | 目的 | 示例值 |
| :--- | :--- | :--- |
| `  --btn-radius ` | 圆角 | `8px` |
| `  --btn-shadow ` | 按钮阴影 | `0 2px 8px rgba(0,0,0,0.2)` |
| `  --btn-transition ` | hover 动画 | `0.2s ease` |
| `  --btn-font-weight ` | 按钮字体粗细 | `500` |

#### 4.1.5 图标系统

| 变量名称 | 目的 | 示例值 |
| :--- | :--- | :--- |
| `  --icon-s ` | 小图标（证据池） | `16px` |
| `  --icon-m ` | 标准图标（导航） | `20px` |
| `  --icon-l ` | 大图标（设置页） | `24px` |
| `  --icon-color ` | 默认色 | `var(--text-primary)` |
| `  --icon-hover ` | 悬浮变色 | `var(--accent)` |

#### 4.1.6 动画与过渡

| 变量名称 | 目的 | 示例值 |
| :--- | :--- | :--- |
| `  --transition-s ` | 快动画，如 hover | `0.15s` |
| `  --transition-m ` | 标准动画，如加载 | `0.3s` |
| `  --transition-l ` | 慢动画，如发光特效 | `0.6s` |

#### 4.1.7 进度条（线性流程条）

| 变量名称 | 目的 | 示例值 |
| :--- | :--- | :--- |
| `  --progress-height ` | 条粗细 | `4px` |
| `  --progress-bg ` | 背景灰 | `\#333333` |
| `  --progress-fill ` | 前进色，霓虹青 | `var(--accent)` |
| `  --progress-radius ` | 条头圆角 | `2px` |

#### 4.1.8 圆角 & 阴影通用

| 变量名称 | 目的 | 示例值 |
| :--- | :--- | :--- |
| `  --radius-s ` | 小圆角 | `4px` |
| `  --radius-m ` | 标准圆角（卡片、按钮） | `8px` |
| `  --radius-l ` | 大圆角 | `12px` |
| `  --shadow-lv1 ` | 轻阴影 | `0 1px 4px rgba(0,0,0,0.15)` |
| `  --shadow-lv2 ` | 重阴影（悬浮时） | `0 4px 12px rgba(0,0,0,0.3)` |
| `  --shadow-glow ` | 发光特效（图标、标题） | `0 0 16px rgba(0, 245, 255, 0.4)` |

#### 4.1.9 验收标准 & 提示（新增为正式要求）

  * 所有生成代码必须 **100%** 使用上述变量替换硬编码值。
  * **示例：** 按钮的 CSS 必须写成 `.btn { padding: var(--padding-btn); border-radius: var(--btn-radius); }`，**而非** `padding: 12px 24px; border-radius: 8px;`。
  * 任何不符合变量化标准的提交都将被视为不合格。

-----

### 4.2 数据管理与安全

| 维度 | 要求 | 验收标准 |
| :--- | :--- | :--- |
| **数据安全** | 确保用户隐私。 | 用户上传的数据默认**只存储在本地浏览器**，未经用户明确授权，数据**绝不**上传至 DataPrism 服务器。 |
| **数据冲突** | 解决多文件同名字段冲突。 | 当导入文件出现同名字段时，应用应**提示用户**进行字段重命名或定义合并策略。 |

-----

### 4.3 项目管理与可扩展性

| 维度 | 要求 | 验收标准 |
| :--- | :--- | :--- |
| **首次使用** | 清晰的用户引导。 | 首次加载应用时，界面需提供清晰的**上传文件/创建项目**引导和 **Prompt 库**的介绍。 |
| **可扩展性** | 预留升级接口。 | 基础架构需预留接口，支持未来集成更强大的 AI 模型或第三方数据连接。 |
| **语言环境 (新增)** | **产品 UI 界面必须使用中文。** 所有的按钮、标签、提示信息等都必须是中文文本。 | UI 界面 100% 中文化，不出现未经翻译的英文词汇。 |

-----

### 4.4 性能与稳定性

| 维度 | 要求 | 验收标准 |
| :--- | :--- | :--- |
| **性能** | 核心分析操作响应迅速。 | 在处理 50,000 行以内的数据集时，核心分析操作（如生成假设、一键清洗）响应时间应 **低于 2 秒**。 |
| **大文件性能** | 支持 Google Sheets 级别的数据规模。 | **直接加载**：≤ 50,000 行；**抽样加载**：≤ 1,000,000 行；**虚拟滚动**：渲染任意规模数据表，帧率 ≥ 30 FPS。 |
| **错误处理** | 友好提示和恢复机制。 | 当 **Pyodide 内存溢出**或数据解析失败时，应用必须提供**非崩溃式**的友好中文错误提示。 |
| **Google Sheets 集成** | 可靠的 API 调用与错误处理。 | 必须正确处理网络错误、权限不足、配额超限等场景，并向用户清晰提示解决方案。 |

-----

### 4.5 可扩展性与流程编排（n8n 借鉴新增）

| 维度 | 要求 | 验收标准 |
| :--- | :--- | :--- |
| **流程可视化** | 整个分析流程必须以**可编辑的管道 (Pipeline)** 形式展示，允许用户在主要节点间插入或调整**自定义的转换模块**。 | 流程视图必须清晰展示每个节点的状态 (已执行/待执行/错误)，并支持节点的拖动与重排。 |
| **数据转换模块化** | Prompt 库 (`PromptSchema`) 必须实现**功能模块的节点化管理**，允许前端将复杂的清洗和特征工程封装为独立、可复用的模块。 | 任何遵循 `TransformationSchema` 的操作（如标准化、填充缺失值）必须能被 Prompt 库引用，并以**独立模块节点**的形式插入到分析流程中。 |
| **报告结果外发** | 必须预留接口，支持用户将最终的**结构化分析结果**（如摘要、统计指标、图表数据）**导出或通过 Webhook 外发**，但不允许原始数据外传。 | 报告模块必须提供 **JSON/Webhook 配置界面**，仅允许外发分析结果的 Schema 数据。 |

-----

## 5\. 技术实施方案与建议

### 5.1 核心技术栈与环境

  * **数据处理引擎 (前端本地)：** 必须采用 **Pyodide/WebAssembly (Wasm)** 运行 **Pandas/Numpy**。
  * **Wasm 辅助库：** 引入 **Apache Arrow 的 Wasm 版本**，以高效处理 **Parquet/HDF5**。

### 5.2 LLM 服务集成与安全

  * **API Gateway：** 用于集中管理 LLM API Key，执行**配额限制**、**安全审计**和**缓存**（如需实现，应为独立服务）。
  * **数据安全原则：** 用户的原始数据**绝不**通过 API Gateway 流向外部 LLM 服务。
  * **LLM 语言约束（新增）：**
      * **AI 输出要求：** LLM 生成的分析洞察、摘要、报告和所有文本回复，其主要语言必须是**中文**。
      * **Prompt 模板：** 所有的预设 Prompt (`full_prompt`) 模板应以**中文指令**为主，以确保 AI 理解并以中文回复。

### 5.3 性能与数据流优化 (Wasm Core)

  * **异步计算隔离（性能核心）：** 强制将所有计算密集型的数据操作（Prompt 生成的代码执行）转移到 **Web Workers** 中运行，以避免阻塞 UI 线程。
  * **大型数据处理策略（JOIN/合并）：** 采用 **DataFrames Chunking（分块处理）** 技术，并严格执行 **大文件抽样提示** 的用户引导流程。
  * **版本控制机制：** 实施 **差分存储（Delta Storage）**：历史记录/回滚机制仅记录每次操作的 **Pandas 操作指令序列**，而不是完整数据副本。
  * **数据状态同步（补充）：**
    1.  每完成一次清洗或特征工程操作，Wasm Worker 必须向主线程发送**更新后的 `ColumnMetadata`**。
    2.  主线程收到更新后，需**立即刷新**数据编辑器中的统计信息。
    3.  Wasm Worker 内部的 DataFrame 实例必须被视为**单一数据源**，并进行**版本化锁定**，确保分析时始终针对最新状态的数据进行操作。

### 5.4 关键功能实现细节

  * **代码沙盒：** 用于执行 Python 代码的 Wasm 环境必须是**安全隔离**的沙盒环境。
  * **图表渲染：** Python 端生成的图表数据（如 Plotly/Altair JSON）应被捕获，并在前端使用高性能的 JS 库进行渲染。
  * **主题变量管理：**
      * **CSS 变量定义：** 必须在全局 CSS 或样式系统中定义一套 CSS 变量（如 `:root { --color-background-primary: #...; }`），对应 `ThemeSchema` 中的 `colors` 字段。
      * **主题切换机制：** 推荐使用 **React Context** 来管理当前的主题 ID。当用户切换主题时：
        1.  更新 `localStorage` 中的主题 ID。
        2.  更新 React Context 中的主题状态。
        3.  **核心实现：** 在页面的根元素（如 `<body>` 或 `#root` div）上，通过 JavaScript 动态注入或修改 CSS 变量的值，或者动态切换一个 **CSS 类名**（如 `<body class="theme-neufuture">`），让 CSS 文件中的变量块生效。
  * **图表颜色同步：** 由于图表渲染（Plotly.js）是在前端完成的，图表的**背景色、文字颜色、坐标轴颜色** 必须从当前激活的 **CSS 变量** 中读取，实现图表与 UI 主题的无缝同步。

### 5.5 新增：关键风险与兜底方案

1.  **Pyodide 冷启动巨慢（5-15s）**

      - 首次加载必须全屏「正在唤醒 Python 内核…」+ 进度条 + 精美骨架屏
      - 预热策略：在 IndexedDB 中缓存 Pyodide 内核，第二次打开 $\le 3\text{s}$

2.  **内存炸裂风险**

      - $> 100\text{k}$ 行强制抽样 10%（不可跳过）
      - 界面顶部常驻红色警告条：「您正在使用抽样模式（仅 10% 数据），结论仅供参考」

3.  **LLM API 费用失控**

      - 每条 Prompt 必须标注预计 token（前端硬编码）
      - 相同数据 + 相同 Prompt 必须缓存 24h
      - 提供「省钱模式」开关：自动走 Grok-4 Fast 或 Claude-3.5-Haiku（最便宜的）

4.  **图表渲染**

      - 明确指定：Python 端统一输出 Plotly JSON $\rightarrow$ 前端强制用 Plotly.js 渲染（性能最稳）
### 5.6 流程节点化与模块实现（n8n 借鉴新增）

#### 5.6.1 Prompt 库与节点化 (`PromptSchema` 增强)

为了实现 **数据转换的模块化**，`PromptSchema` 接口需要具备区分**流程控制**和**数据操作**的能力。

  * **Prompt 类别扩展：** `PromptSchema` 的 `category` 字段应新增 `流程控制 (Flow)` 和 `数据转换 (Transform)` 两个类别。
  * **模块化引用：** 任何需要 Wasm 执行的**数据清洗或特征工程**操作，都应被封装为一个独立的 **`Transform`** 模块，该模块遵循以下原则：
      * **输入：** 接收当前 DataFrame 的版本 ID 和参数。
      * **执行：** 在 Web Worker 中执行 Pyodide 代码。
      * **输出：** 返回新的 DataFrame 版本 ID 和更新后的 `ColumnMetadata`。
  * **流程编排层：** 前端需维护一个状态数组 (`PipelineSchema`) 来存储当前的分析流程，该数组包含所有节点（包括 `Transform` 和 `Analysis`）的 ID 和参数。

#### 5.6.2 Webhook 导出实现

  * **数据隔离：** 明确规定，只有**经过 AI 总结或结构化处理**的报告数据（例如，报告摘要、统计值），才能被放入 Webhook 负载中。**原始数据集的任何部分都不得被包含。**
  * **技术栈：** Web Worker 负责执行 `fetch()` 请求，向用户配置的 Webhook URL 发送 JSON 格式的结构化报告数据。

### 5.7 核心数据结构更新（需在 0.2 节同步）

为了支持流程节点化，我们需要一个用于定义分析管道的新结构 分析管道结构 (`PipelineSchema`)

该接口定义了用户当前项目中的分析流程节点序列。


## 6\. 用户故事（Grok 新增，必写！）

| ID | 作为... | 我希望... | 以便于... | 优先级 |
|---|---|---|---|---|
| US-01 | 数据分析师 | 上传 CSV 后，能先看到详细的统计信息和清洗建议 | 我可以先整理数据，再开始做假设 | P0 |
| US-02 | 数据分析师 | 清洗完成后，AI 基于干净数据立即给我 5 个结构化的商业假设 | 我知道数据够不够，不做无用功 | P0 |
| US-03 | 数据分析师 | 所有图表、代码、洞察都能一键扔进证据池 | 出报告时不找东找西 | P0 |
| US-05 | 数据分析师 | 报告页可以像 Notion 一样修改，改完对应证据自动标黄 | 我能背锅，也能改错 | P0 |
| US-06 | 数据分析师 | 最后点「人工签字」按钮变绿，才算完事 | 老板一看就知道这是我审过的 | P0 |
| US-07 | 数据分析师 | 整个产品纯本地运行，不联网也行 | 数据敏感的项目也能用 | P0 |

-----

## 7\. MVP 功能切割（Grok 强砍版 - 40天可发版）

**MVP 铁线（必须做的，其他全砍到 v1.1）**：

1.  文件导入（CSV/XLSX + Parquet）。
2.  **Google Sheets 集成（核心差异化功能）**：通过 URL 导入 Google Sheets 数据，支持公开/私有 Sheets。
3.  **大文件处理能力提升**：支持 100 万行数据（抽样模式）+ 虚拟滚动 + 流式解析。
4.  数据表 + ColumnMetadata 统计展示 + 手动改类型。
5.  清洗模块（3-5 种常见清洗 Prompt + 一键应用 + 版本回溯）。
6.  分析假设（AI 生成 5 条 + 用户自定义 + 结构化拆解确认）。
7.  Prompt 库（内置 8-10 条，不可 CRUD，先写死在 JSON）。
8.  分析看板（AI 洞察 + 代码执行 + 图表）。
9.  证据池（拖拽 + 一键插入报告）。
10. 报告页（可编辑 + 人工签字绿按钮 + 导出 HTML/Markdown）。
11. 大文件抽样强制弹窗 + Pyodide 冷启动加载动画。
12. **主题切换功能：** 实现**一套完整的 CSS 颜色变量定义**，并在上方导航栏提供**主题切换器**，支持在 **Neufuture 暗夜版** 和 **另一个预设主题** 之间切换。