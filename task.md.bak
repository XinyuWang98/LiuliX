# DataPrism 开发规划与任务清单

> [!IMPORTANT]
> 用户规则遵从：本规划严格基于 `PRD.md` 与当前代码库的对比差异制定。优先级依据 PRD 第7节 "MVP 铁线" 及技术依赖关系分配。

## Phase 0: 基础设施与 AI 核心 (已完成)
- [x] **AI 服务基础设施** (PRD 5.2)
    - 多模型支持 (Gemini, Grok, Claude, DeepSeek)。
    - 健壮的错误处理与 I18n 注入。
    - 验证构建与稳定性通过。
- [x] **项目结构与基础 UI** (PRD 2.1, 3.0)
    - 带主题切换的导航栏。
    - 左侧边栏与文件上传器 (本地)。
    - 基础数据编辑器 (`DataViewer`, `DataTable`)。
- [x] **类型系统** (PRD 0.2)
    - `PromptSchema`, `ColumnMetadata`, `HypothesisSchema`, `ThemeSchema`.

---

## Phase 1: 数据集成 (当前焦点 - P0)

**理由**: 没有数据就没有分析。Google Sheets 是 MVP 中定义的“核心差异化功能”(PRD 7)。

- [ ] **Google Sheets 集成** (PRD 2.2) <!-- id: 101 -->
    - **优先级**: **P0 (关键)**
    - **理由**: MVP 需求中的核心差异点。
    - **风险**: 跨域问题 (CORS), API 配额限制, 公开表格 HTML 解析逻辑复杂。
    - **技术方案**: 优先使用 CSV 导出链接 Hack + API Key (私有表格)。避免复杂的纯后端 OAuth。
    - 任务细分:
        - [ ] 侧边栏新增“从 Google Sheets 导入”按钮。
        - [ ] 实现 `GoogleSheetModal` 用于 URL 输入。
        - [ ] 实现 `fetchSheetData` 工具函数 (CSV/API 策略)。

- [ ] **大文件处理与抽样** (PRD 2.2, 4.4) <!-- id: 102 -->
    - **优先级**: **P0 (关键)**
    - **理由**: 防止大文件导致浏览器崩溃 (OOM)。PRD 要求超 5k 行必须警告。
    - **风险**: Worker 内存管理不当即内存泄漏。
    - **技术方案**: 实现流式解析 (PapaParse stream) 和强制抽样逻辑。
    - 任务细分:
        - [ ] 在 `FileUploader` 中实现 50k 行 / 50MB 检测逻辑。
        - [ ] 添加“立即抽样”与“强制导入”的模态框。

---

## Phase 2: 数据探索与清洗 (P0)

**理由**: "数据编辑器" 和 "清洗" 是 PRD 2.3 节定义的用户流首步。

- [ ] **手动数据类型修正** (PRD 2.3.1) <!-- id: 201 -->
    - **优先级**: **P0**
    - **理由**: 用户需要在 AI 分析前修正错误类型 (如将字符串 "100" 改为数字)。
    - **风险**: 低。
    - **技术方案**: 更新 `ColumnMetadata` 并触发 Pyodide 重解析/重渲染。

- [ ] **缺失值可视化** (PRD 2.3.1) <!-- id: 202 -->
    - **优先级**: **P1**
    - **理由**: PRD 明确要求的视觉增强 (空值用灰色条块填充)，提升 UX。
    - **紧急度**: 中。

- [ ] **AI 数据清洗建议** (PRD 2.3.1) <!-- id: 203 -->
    - **优先级**: **P0**
    - **理由**: 核心价值主张。一键应用清洗建议。
    - **技术方案**: AI 生成 Python 代码 -> Pyodide 执行 -> 更新 DataFrame。

---

## Phase 3: 分析与假设 (P0)

**理由**: 产品的"智能"核心层。

- [ ] **假设生成与结构化** (PRD 2.3.2) <!-- id: 301 -->
    - **优先级**: **P0**
    - **理由**: MVP "铁线" 第6条。
    - **风险**: AI 输出质量 (必须严格遵循 Schema)。
    - **技术方案**: 强制 `HypothesisSchema` JSON 输出。

- [ ] **分析看板** (PRD 2.3.3) <!-- id: 302 -->
    - **优先级**: **P0**
    - **理由**: 展示结果的地方。
    - **技术方案**: 渲染 Python 输出的 Plotly JSON。

---

## Phase 4: 工坊与报告 (P0)

**理由**: 产品的"闭环"环节。

- [ ] **智能工坊 (右侧边栏)** (PRD 2.4) <!-- id: 401 -->
    - **优先级**: **P1**
    - **理由**: "聊天"和"证据池"的容器。Day 1 可以简化，但需搭建结构。
    - **技术方案**: `SmartWorkshop.tsx`.

- [ ] **项目分析报告 (Notion 风格)** (PRD 2.3.4) <!-- id: 402 -->
    - **优先级**: **P0**
    - **理由**: "人工签字"(绿按钮) 是关键用户故事 (US-06)。
    - **风险**: UI 实现极其复杂 (ContentEditable + 拖拽)。
    - **复杂度**: 高。需要 Canvas 级或 Block 级编辑器。

---

## 技术债务与基建

- [ ] **Prompt 库配置化** (PRD 2.1) <!-- id: 901 -->
    - **优先级**: **P1**
    - **理由**: 将硬编码 Prompt 移至外部 JSON 配置文件维护。

- [ ] **工具函数单元测试** <!-- id: 902 -->
    - **优先级**: **P2**
    - **理由**: 确保解析器和数据处理逻辑健壮。
